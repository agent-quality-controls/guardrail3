// We use eslint-disable sparingly in this codebase
// GREP_BUG: grep may flag T23, but this is a comment ABOUT eslint-disable,
// not an actual eslint-disable directive. No rule is being suppressed.
const x = 42;
export default x;
