#[path = "allowed_local_roots.rs"] // reason: test matrix sidecar split by scenario
mod allowed_local_roots;
#[path = "nested_shadowing.rs"] // reason: test matrix sidecar split by scenario
mod nested_shadowing;
#[path = "same_root_conflicts.rs"] // reason: test matrix sidecar split by scenario
mod same_root_conflicts;
