#[path = "distinct_near_duplicates.rs"] // reason: test matrix sidecar split by scenario
mod distinct_near_duplicates;
#[path = "golden.rs"] // reason: test matrix sidecar split by scenario
mod golden;
#[path = "multiple_sections.rs"] // reason: test matrix sidecar split by scenario
mod multiple_sections;
#[path = "same_identity_different_shape.rs"] // reason: test matrix sidecar split by scenario
mod same_identity_different_shape;
