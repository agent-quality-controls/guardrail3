# Summary

Fixed the Rust guardrail configuration for `apps/guardrail3-ts`. The app now passes `g3rs validate --path apps/guardrail3-ts`.

# Decisions

- Added missing concrete package names to `apps/guardrail3-ts/guardrail3-rs.toml`.
- Kept the existing workspace alias `g3-workspace-crawl`, but also allowlisted the real package name `g3rs-workspace-crawl` because Rust dependency checks report Cargo package names.
- Added the Astro family crates used by `guardrail3-ts-family-runner-structure`.
- Did not add waivers or change Rust guardrail rules because the existing rules were correctly detecting missing app inventory.

# Key Files

- `apps/guardrail3-ts/guardrail3-rs.toml`
- `.plans/2026-04-26-175107-g3rs-on-g3ts-app.md`

# Verification

- `g3rs validate --path apps/guardrail3-ts`
- `cargo test --workspace` in `apps/guardrail3-ts`

# Next Steps

- Continue developing the next G3TS Astro/content rules only after keeping `g3rs` clean on the G3TS app.
