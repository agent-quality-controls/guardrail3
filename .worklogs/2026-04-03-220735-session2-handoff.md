# Session 2 handoff

**Date:** 2026-04-03 22:07

## What was done

### ARCH facade cleanup (continued from session 1)
- Cleaned 145 split-rule facades (helpers → tests/helpers.rs)
- Split 56 unsplit rules (deny 27 + release 29)
- Moved check() from 12 family runtime lib.rs → run.rs
- Split 12 test_support lib.rs → support.rs
- Excluded legacy/ from validation
- Retired RS-LIBARCH (moved to legacy, unwired, cleaned stale worktrees)
- Fixed test assertion mismatches from error message clarity commits (clippy, code, deps, release, cargo)
- ARCH errors: 3410 → 2245

### Family extraction design
- Designed architecture: families as independent packages behind typed inputs
- Audited all families: what files they read, what they parse, cross-domain access
- Audited all rules: ownership, misplacements, overlaps
- Decided: cargo_toml crate for Cargo.toml, rust-toolchain-file for toolchain, raw strings for family-owned configs
- Decided: standalone parser crates for clippy.toml, deny.toml, rustfmt.toml, nextest.toml, mutants.toml

### First parser crate: rustfmt-toml
- Built packages/rustfmt-toml/ with ~80 typed fields, catch-all for unknown
- Full guardrail compliance: zero allows for clippy lints, full baseline clippy.toml + deny.toml
- Fixed all fixable warnings (5 fixed, 6 genuinely unavoidable)
- 5 tests pass, zero clippy warnings

## What to do next

### 1. Build remaining 4 parser crates
Use rustfmt-toml as template for structure, lint policy, deny.toml, clippy.toml.

Order: mutants-toml (simplest) → clippy-toml → nextest-toml → deny-toml (most complex)

For each:
- Copy workspace Cargo.toml, clippy.toml, deny.toml, rust-toolchain.toml from rustfmt-toml
- Create crate with typed structs from the plan (.plans/2026-04-03-210000-config-parsers.md)
- Implement FromStr, from_path, Error type
- Write tests
- Run guardrail3, fix all fixable warnings
- Commit

### 2. Fix remaining test failures
- garde (25 fail): message clarity assertion mismatches
- fmt (14 fail): same
- toolchain (18 fail): same
- hexarch (53 compile errors): test helper import issues from facade cleanup

### 3. Continue ARCH error reduction (2245 remaining)
- ARCH-04 (1449): tests/mod.rs helpers, infrastructure mod.rs
- ARCH-02 (343): runtime lib.rs test helpers, assertions lib.rs, infrastructure
- ARCH-06 (372): shared flag dep chain analysis
- ARCH-08 (46): feature gates on new files

## Key files
- `.plans/2026-04-03-201700-family-extraction.md` — extraction architecture
- `.plans/2026-04-03-210000-config-parsers.md` — parser crate designs with full type definitions
- `packages/rustfmt-toml/` — reference implementation for parser crates
- `.worklogs/2026-04-03-185024-session-handoff.md` — session 1 handoff (ARCH details)
