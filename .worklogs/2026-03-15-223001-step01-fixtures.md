# Step 01: 50 adversarial grep-attack fixtures

**Date:** 2026-03-15 22:30
**Scope:** tests/fixtures/grep-attacks/ — 50 fixtures + manifest

## Summary
50 adversarial fixtures across 5 categories designed to expose grep-based false positives. Each file contains a pattern (allow, unsafe, todo, unwrap, eslint-disable, etc.) in a non-code context (strings, comments, field names) where grep incorrectly flags it but AST parsing should not.
