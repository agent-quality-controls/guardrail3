# Retire RS-LIBARCH family

**Date:** 2026-04-03 13:09

## Summary
Removed the RS-LIBARCH family entirely. Its generic rules (01-03) were
already absorbed into RS-ARCH. The remaining layered-shape rules (04-11)
had zero output on the codebase — no errors, no warnings, no inventory.

## Decisions
- Moved source to `legacy/apps/guardrail3/crates/app/rs/families/libarch/`
- Kept `TopologyOwner::Libarch` in placement classification — it's a
  topology concept for package-zone roots, not the family itself.
- Removed `Libarch` variant from `RustValidateFamily` enum and all match sites.
- Removed `map_manifest_roots_for_family` from mapper (was libarch-only).
- Removed `libarch = true` from all test config strings and guide.rs.

## Key files
- `apps/guardrail3/Cargo.toml` — workspace members removed
- `crates/domain/validation-model/src/families.rs` — enum variant removed
- `crates/app/rs/family_mapper/src/rs/mod.rs` — mapper method removed
- `crates/app/rs/runtime/src/lib.rs` — runner removed
- `crates/domain/config/types.rs` — config field removed
