# Add nextest-toml parser crate

**Date:** 2026-04-03 23:20

## Summary
Fourth parser crate. NextestConfig with profile map, NextestProfile with
private fields + accessors, TimeoutConfig untagged enum, TimeoutDetail
with private fields + accessors. 10 tests, zero clippy, guardrail-compliant.
