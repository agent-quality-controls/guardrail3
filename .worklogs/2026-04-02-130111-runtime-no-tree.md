# Runtime: tree removed, all runners use FamilyView

**Date:** 2026-04-02 13:01

## Summary
Complete rewrite of runtime context and all 16 runners. ProjectTree removed
from RustRunContext. All runners build FamilyView from ctx.legality instead
of ctx.tree. No tree access in any runner.

## Changes
- context.rs: removed tree field, added legality field
- runners.rs: all 16 runners rewritten to use FamilyView::build()
- Removed all surface-building helpers (topology_surface, workspace_surface, etc.)
- Removed all tree-dependent helpers (code_surface_file_rels, hook_file_rels, etc.)
- New helpers operate on dir_structure BTreeMap instead of ProjectTree
- runtime/Cargo.toml: added family_view dep, hooks-shared now requires routing

## Verification
Runtime compiles clean (routing-only, no families).
Family errors are all "RsProjectSurface not found" — expected, separate step.
