# Add --dump-dir to diff command for verification

**Date:** 2026-03-19 21:20
**Scope:** cli.rs, diff.rs, main.rs

## Summary
Added `--dump-dir <path>` flag to `rs diff` and `ts diff` commands. When provided, writes all generated files to the directory for manual inspection instead of just showing the summary. Used to verify diff output matches reality.

## Verification
Dumped to /tmp and compared every file:
- deny.toml: identical (no changes needed — correct)
- clippy.toml: identical
- rustfmt.toml: identical
- rust-toolchain.toml: identical
- pre-commit: 1 line differs (workspace root path) — correct
- Generated deny.toml contains all 7 regex bans — generate is producing them correctly
