# Coverage map: show covered dirs per config + collapse fix

**Date:** 2026-03-19 11:21
**Scope:** coverage/engine.rs

## Summary
Changed ConfigInstance from `covers_dirs: u32` (count only) to `covers: Vec<String>` (collapsed top-level dirs). Both covers and uncovered now show meaningful directory names. Collapse stops at one level below project root to keep top-level dirs visible.
