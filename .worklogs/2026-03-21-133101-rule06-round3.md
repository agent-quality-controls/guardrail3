# Rule 06 round 3: check bug fix + /test-attack convergence

**Date:** 2026-03-21 13:31

## Check bug fixed

**is_gitkeep_only**: check_06 line 55 used `has_gitkeep` which accepted `.gitkeep` + source files as a valid placeholder. Fixed: new `is_gitkeep_only()` helper in arch_helpers.rs verifies the subdir contains ONLY `.gitkeep` and nothing else (no other files, no subdirs).

## Other fixes

- TS arch test fixture (`adversarial_categories.rs`): `setup_ts_monorepo` now creates full hex arch with ports/ and inbound/outbound
- TS arch unit test (`ts_arch_checks_test.rs`): switched from broken StubFs to RealFileSystem for `t_arch_01_app_with_full_structure`
- `assert_no_ts_apps` helper now checks "portal" (3rd TS app in golden)
- `crates_dir_exists_but_empty`: fixed `&errors` → `&r6` pattern deviation
- `dangling_symlink_in_crates_dir`: exact count assertion + removed stale CHECK BUG comment
- Stale comment in `maximally_complex_single_container` fixed

## Tests added (8 new, 43 total rule_06)

- `gitkeep_plus_source_files_fires_everywhere` — .gitkeep+source in all 21 locations
- `gitkeep_plus_subdir_fires` — .gitkeep+subdir
- `subdir_with_only_gitkeep_everywhere` — strengthened to all 21 locations
- `crates_dir_exists_but_empty_everywhere` — strengthened to all 21 locations
- `crates_is_file_not_dir_in_leaf` — crates as file not dir
- `gitkeep_as_directory_fires` — .gitkeep as dir not file
- `gitkeep_plus_flat_files_no_subdirs_fires` — .gitkeep + README.md (file_names.len() != 1 branch)
- `orphan_inside_triple_nested_hex` — depth-3 recursion orphan

## /test-attack round 3 verdict

0 CHECK BUG, 0 MUST FIX, all truth table scenarios covered, full branch coverage. Converged.

## Test counts: 805 passing, 5 intentional failures (rules 07-11)
