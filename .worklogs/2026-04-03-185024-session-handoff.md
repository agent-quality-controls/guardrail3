# Session handoff — ARCH-04 facade cleanup + error message fixes

**Date:** 2026-04-03 18:50

## What was done this session

### Facade cleanup (ARCH-02 / ARCH-04)
- Moved #[cfg(test)] helpers from 145 split-rule mod.rs → tests/helpers.rs
- Split 27 deny + 29 release unsplit rules into rule.rs + tests/helpers.rs
- Moved pub fn check() from 12 family runtime lib.rs → run.rs
- Split 12 test_support lib.rs into facade + support.rs
- Excluded legacy/ from arch validation (guardrail3.toml)
- Retired RS-LIBARCH family (moved to legacy/, unwired everywhere)
- Cleaned 18 stale agent worktrees (source of libarch zombie)

### Error message clarity (delegated agents, separate work stream)
- Improved messages across all families with remediation instructions
- Some severity changes: Info→Warn (tightening), two relaxations (CODE-31 <5 fields, HEXARCH-16 documented bypasses)
- Fixed assertion mismatches for clippy, code, deps, release, cargo

### Arch error reduction
- **Start of session:** 3410 errors
- **End of session:** 2245 errors (-1165, 34% reduction)

## Current arch error counts

| Rule | Count | What remains |
|------|------:|--------------|
| ARCH-04 | 1449 | tests/mod.rs helpers (~119), infrastructure mod.rs (~56), app infra (~12), 4 unsplit rules |
| ARCH-06 | 372 | Non-child deps missing shared=true — needs dep chain analysis |
| ARCH-02 | 343 | Family runtime lib.rs (#[cfg(test)] helpers ~148), assertions lib.rs (marker imports ~43), other infra (~152) |
| ARCH-08 | 46 | Feature gates needed on new run.rs/support.rs crates |
| ARCH-07 | 26 | Complex crates exceeding thresholds |
| ARCH-05 | 8 | Real boundary crossings |
| ARCH-03 | 1 | One TS validator foo.rs convention |

## Test status per family

| Family | Status | Notes |
|--------|--------|-------|
| clippy | **158/158 pass** | Fixed this session |
| code | **230/230 pass** | Fixed this session, CODE-26/27 retired |
| cargo | **94/94 pass** | Fixed this session |
| deny | **146/146 pass** | Fixed this session |
| release | **266/266 pass** | Fixed this session |
| deps | **73/73 pass** | Fixed this session |
| test | **122/122 pass** | Fixed this session |
| garde | 81 pass, **25 fail** | Message clarity assertions not yet updated |
| fmt | 16 pass, **14 fail** | Message clarity assertions not yet updated |
| toolchain | 30 pass, **18 fail** | Message clarity assertions not yet updated |
| topology | 62 pass, **3 fail** | Pre-existing (excluded fixtures) |
| hexarch | **compile error (53)** | Pre-existing test helper import issues from facade cleanup |

## What to do next (priority order)

### 1. Fix remaining test assertion failures (garde 25, fmt 14, toolchain 18)
Same pattern as clippy/code/deps/release/cargo: error message clarity commits
changed messages/severity but didn't update assertions. Mechanical — read the
failing test, update expected string/severity in the assertion module.

### 2. Fix hexarch test compilation (53 errors)
The hexarch family has a different test pattern — tests/mod.rs files import
`results_for_test_root` and `results_for_test_tree` from the rule module via
`super::`, plus 25 `#![allow(...)]` inner attributes placed after `mod helpers;`.
Needs manual per-rule fixes, not scriptable.

### 3. Continue ARCH-04 (1449 remaining)
Next buckets:
- **tests/mod.rs helpers (119 dirs)** — move helper functions from tests/mod.rs
  to tests/helpers.rs. Same pattern as rule facades but risk of cascading import
  changes (tests use grouped `use super::{...}` imports).
- **Infrastructure mod.rs (56 dirs)** — facts/, parse/, inputs/, discover/ with
  implementation. Each needs a sibling file, unique per module.

### 4. Continue ARCH-02 (343 remaining)
- **Family runtime lib.rs #[cfg(test)] (148)** — test helpers still in lib.rs.
  Need to move to a crate-level test module or test_helpers.rs.
- **Assertions lib.rs marker imports (43)** — `use X as _;` flagged as private use.
  Rule question: should ARCH-02 allow `use X as _;` in lib.rs?
- **Other infrastructure (152)** — various small crates with implementation in lib.rs.

### 5. ARCH-06 (372) — shared flag
User flagged this as "probably means the dep chain is wrong." Needs analysis of
which crates are non-child dependencies and whether they genuinely need shared=true
or the dep structure should change.

## Known hazards
- **Libarch zombie**: agents in worktrees or with stale git state keep restoring
  `apps/guardrail3/crates/app/rs/families/libarch/`. Always `git clean -fd` after
  agent runs. All stale worktrees have been cleaned this session.
- **Grouped imports**: many test files use `use super::{A, B, C}` where some items
  come from helpers and some from mod.rs. Narrowing visibility requires splitting
  these grouped imports — high blast radius for low benefit.
- **Error message agents**: the delegated message clarity agents changed severity
  levels and removed .as_inventory() without updating assertions. Three families
  (garde, fmt, toolchain) still have broken assertions from this.

## Key files for context
- `.plans/2026-04-01 ISSUES.md` — original arch rule plan
- `.plans/2026-04-02-230917-arch04-mod-facade-split.md` — ARCH-04 approach
- `.plans/2026-04-03-174546-test-fix-plan.md` — test assertion fix plan
- `guardrail3.toml` — excluded_paths includes "legacy" now
- `apps/guardrail3/crates/app/rs/families/arch/crates/runtime/src/facts/facade_surface.rs` — the ARCH-02/04 detection engine
