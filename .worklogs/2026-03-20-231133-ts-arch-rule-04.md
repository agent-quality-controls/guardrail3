# TS-ARCH-01 Rule 04 Tests

## Summary
51 tests for TS hex arch rule 04 (loose files in container dirs). 3 adversarial rounds converged. Covers admin, portal (outer + 2 inner hexes), all container types, structural dirs.

## Decisions
- `loose_file_errors` filter: simple title-contains "loose files". Works because loose-file errors have a distinct title pattern from other T-ARCH-01 sub-rules.
- `assert_standard` / `assert_standard_admin` helpers for multi-app vs admin-only tests.
- Double-fire fix tested: container with only loose files (no subdirs) gets "empty container" not "loose files" — verified by explicit test.
- Precondition guards added to idempotent_results and per_app_attribution_admin to prevent vacuous passing.
- Inner hex coverage: loose files in payments/modules/ root, payments/modules/adapters/ structural dir, all inner hex containers.

## Bugs found (not fixed — tests only)
- RS `idempotent_results` (rule_04) missing precondition guard (would pass vacuously if check_loose_files were no-op). TS version has the guard.

## Key files
- `tests/unit/ts_arch_01/rule_04.rs` — 51 tests
- `tests/unit/ts_arch_01/helpers.rs` — removed unused assert_file_field import

## Next steps
- Rule 05 tests (container not empty)
