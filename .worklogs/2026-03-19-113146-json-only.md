# Coverage maps: JSON only, drop text tree format

**Date:** 2026-03-19 11:31
**Scope:** coverage/engine.rs, all tool modules, cli.rs, main.rs

## Summary
Removed text tree output format from coverage maps. JSON is now the only output. Dropped `--format` CLI flag. Simplified all tool modules from `print_json`/`print_tree` to single `print`. Removed unused `format_details` helper.
