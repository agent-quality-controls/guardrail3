# Rule 06 tests + check_12 empty src fix

**Date:** 2026-03-20 21:29

## Summary
- Rule 06: 25 exhaustive tests written. Design audit found 2 bugs (.gitkeep-only leaf, empty crates/ misdetection).
- check_12: fixed empty src/ detection using is_dir() fallback. src_empty_dir test now passes.
- 246 passing, 5 intentional failures (rules 07-11 unimplemented).

## Bugs found by design audit
1. check_06: .gitkeep-only leaf subdirs rejected (needs fix — test documents the gap)
2. check_06: empty crates/ dir treated as absent (needs fix — metadata instead of list_dir)
3. check_12: empty src/ not detected (FIXED this commit)

## Total RS-ARCH-01: 246 passing tests.
