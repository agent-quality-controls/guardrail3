# Fix all clippy errors and add adversarial category tests

**Date:** 2026-03-17 14:40
**Scope:** 36 clippy fixes across ~20 files, 16 new adversarial tests

## Summary
Fixed all 36 pre-existing clippy errors (Rust 1.93 stricter lints) and added 16 adversarial integration tests for the check categories feature.

## Clippy Fixes
- `map().unwrap_or()` → `map_or()` (json.rs, markdown.rs, text.rs)
- `type_complexity` — suppressed at usage site (double-ref lifetime prevents clean alias)
- `indexing_slicing` → `.get()` / `.first()` (markdown.rs, text.rs)
- `uninlined_format_args` — inlined variables (text.rs)
- `const fn` — added where possible (ast_helpers, ast_visitors, report.rs, test helpers)
- `must_use` — added to `as_inventory()` (report.rs)
- `struct_excessive_bools` — suppressed on `RustCheckCategories` (independent toggles)
- `too_many_lines` — suppressed on validation orchestrators (sequential check logic)
- `too_many_arguments` — suppressed on `run_code_checks` (passes all context)
- `&Option<T>` → `Option<&T>` (validate.rs, mod.rs)
- `partial_pub_fields` — made fields pub (ast_visitors.rs)
- `doc` issues — added backticks, split long first paragraphs
- `arithmetic_side_effects` → `saturating_add` (eslint_check.rs)
- `match arms identical` → merged with `|` (ts_code_analysis.rs)

## Adversarial Tests (adversarial_categories.rs)
16 tests covering all category behaviors:
- Default: core checks present, architecture/garde absent, tests present
- Config enables/disables each category
- CLI flags override config
- Flag isolation (--garde doesn't include architecture)
- Empty/missing [checks] section uses defaults
