# Final hardening — fix all adversarial findings

**Date:** 2026-03-16 12:15
**Scope:** ast_helpers, code_quality_checks, structure_checks, garde_checks, test_quality_checks, TS ast_helpers

## Summary
Fixed 8 adversarial findings, split 3 oversized files, fixed clippy. 361 tests, 0 failures, 0 clippy warnings.

## Fixes Applied
1. `is_cfg_test_attr` — handles compound cfg predicates (all/any/not)
2. `ItemAllowVisitor` — now visits let bindings and match arms (expression-level #[allow])
3. R53 — checks both `[workspace.lints.rust]` and `[lints.rust]`
4. R-GARDE-05 — split derives merged into one DeriveInfo per item
5. R-TEST-07 — accepts `#[ignore = "reason"]` attribute syntax
6. R43 — handles path-qualified macros (std::todo!, core::todo!)
7. `InlineStdFsVisitor` — catches std::fs::File::open and all std::fs:: type methods
8. Clippy — fixed indexing safety in strip_string_literals

## File Splits
- rs/ast_helpers.rs (956→549) + ast_visitors.rs (441)
- ts/ast_helpers.rs (686→301) + ts_code_analysis.rs (422)
