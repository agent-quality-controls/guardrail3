# Fix jargon in TEST-16/17/18

## TEST-16
- Error 1: "proof-bearing owned assertions function" → specific about CheckResult fields and assertion macros
- Error 2: "push guardrail result assertions into owned sibling assertions module" → names the function, says it accesses CheckResult fields directly, says to move to assertions crate

## TEST-17
- "prove through the owned assertions crate" → names function and file, says "uses assertion macros directly", says to call assertions crate instead

## TEST-18
- "route-construction infrastructure" → FamilyMapper or placement wiring
- "canned path/string data" → hardcoded, helpers should accept parameters
- "selects or inspects guardrail findings/results by rule semantics" → filters CheckResult fields by rule ID or severity, move to assertions crate
