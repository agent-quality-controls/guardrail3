# Summary

Cleaned the G3RS behavior replay fixtures through L50 and wrote the next L60 plan.

The L00-L50 fixture set now covers nested-workspace non-selection, merged member Cargo parse failures into L40, raw and typed config parse failures, and missing `cargo-mutants` when mutation policy is active.

# Decisions Made

- Merged `L42-member-cargo-toml-invalid` into `L40-required-inputs-present-invalid` because the combined fixture still exposes every Error/Warn row from both fixtures.
- Added separate L43 and L44 fixtures because malformed `rust-toolchain.toml` plus nightly-only rustfmt policy and typed-invalid `clippy.toml` are mutually exclusive with the existing raw malformed input cases.
- Added `L50-test-cargo-mutants-missing` because `cargo-mutants` is a delegated tool only when mutation testing is active.
- Rejected the release README source-input fixture for L40 because the public release family boundary also emits `g3rs-release/publish-dry-run`, which pollutes the invalid-input layer.
- Normalized Cargo package-cache lock waits in behavior output because they are timing noise from concurrent Cargo processes.

# Key Files

- `.plans/2026-05-13-033126-clean-g3rs-l00-l50-and-plan-l60.md`
- `.plans/2026-05-12-183156-guardrail3-behavior-replay-fixture-migration.md.manifest.toml`
- `behavior/fixtures/g3rs/L00-workspace-root-not-found`
- `behavior/fixtures/g3rs/L40-required-inputs-present-invalid`
- `behavior/fixtures/g3rs/L43-fmt-nightly-key-toolchain-invalid`
- `behavior/fixtures/g3rs/L44-clippy-typed-config-invalid`
- `behavior/fixtures/g3rs/L50-test-cargo-mutants-missing`
- `scripts/behavior/baseline_common.py`

# Verification

- `scripts/behavior/verify-all.sh`
- `python3 -m py_compile scripts/behavior/*.py`
- `git diff --check`
- `g3rs validate-repo`
- `g3rs validate --path apps/guardrail3-rs`

# Next Steps

- Build L60 only from delegated-policy failures that remain visible after delegated tools are present.
- Do not add release README source-input replay until there is a public boundary that can isolate source input without publish dry-run pollution.
