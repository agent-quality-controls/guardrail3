# Fix 5 HIGH Rust findings — adversarially verified

**Date:** 2026-03-19 22:16
**Scope:** allow_checks.rs, ast_helpers.rs, ast_visitors.rs, code_quality_checks.rs, hex_arch_checks.rs

## Fixes (verified in 2 adversarial rounds)

### 1. R32: require `// reason:` prefix (not just any comment)
- `// lol` now fails as R32 Error
- `// reason: justification` passes as R33 Info
- Empty `// reason:` or `// reason:   ` rejected (round 2 fix)
- Case-insensitive (`// Reason:` works)
- Zero-width space unicode trick now caught

### 2. R58: `use std::fs::*` glob import detection
- UseTree::Glob now returns true in use_subtree_is_fs
- Catches: `use std::*`, `use std::{fs::*, io}`, `use std::fs::{self, *}`

### 3. R30: inline mod `#![allow]` detection
- New find_inline_mod_allows() walks ItemMod inner attributes
- Reports correct module path (e.g., `foo::bar`)
- Deeply nested modules caught, external `mod foo;` correctly skipped

### 4. F-04-03: dev-dependencies + build-dependencies in arch check
- check_dependency_flow and check_library_service_boundary now check all 3 sections
- Error message labels which section the violation is in

### 5. R30/R37: cfg_attr(all(), allow(...)) always-true detection
- CfgAttrAllowInfo struct with is_always_true field
- all() with no args detected as always-true → R32 Error
- all(unix) and any(...) correctly stay R37 Info

## Known gap (not fixed)
- Target-specific deps `[target.'cfg(test)'.dev-dependencies]` not parsed (pre-existing)
