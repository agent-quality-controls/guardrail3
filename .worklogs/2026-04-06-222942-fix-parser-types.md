# Add missing typed fields to parser types

**Date:** 2026-04-06 22:29
**Scope:** release-plz-toml-parser, cliff-toml-parser

## Summary
Added `publish: Option<bool>` to `ReleasePlzPackage` and `trim: Option<bool>` to `CliffChangelogSection`. Both fields exist in the real repo config files and were falling to `extra` instead of being typed. Verified both parsers parse the real files correctly.
