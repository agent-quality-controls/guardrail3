# Fix rs init to group crates by top-level app

**Date:** 2026-03-17 21:51
**Scope:** commands/init.rs

## Summary
Init was listing every workspace member as a separate app — including internal hex arch crates (domain, ports, app, adapters). Fixed to group by top-level directory under apps/. Now shows one entry per app (e.g., validator-rust) not per internal crate.
