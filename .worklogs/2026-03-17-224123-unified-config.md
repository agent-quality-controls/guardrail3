# Unified config format: type + per-app checks for both RS and TS

**Date:** 2026-03-17 22:41
**Scope:** init.rs, types.rs, hex_arch_checks.rs, guardrail3.toml

## Summary
Unified Rust and TypeScript config format. Both now use `type` (not `profile`), per-app/package checks (not global), no `layer`/`composition-root`/`pure` exposed to users.
