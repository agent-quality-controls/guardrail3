# TS-ARCH-01 Rule 02 Tests

## Summary
36 tests for TS hex arch rule 02 (modules/ must contain exactly {domain, ports, application, adapters}). 3 adversarial rounds converged.

## Decisions
- Created `rule2_errors` filter to isolate rule 02 errors from other T-ARCH-01 sub-rules (rule 03 inbound/outbound, rule 05 containers, etc.). Filter matches by title pattern: missing top-level dirs, unexpected dirs in modules/, loose files in modules/ root.
- Created `assert_standard` helper bundling `assert_all_mention_admin` + `assert_file_field_modules` + `assert_no_rust_apps` + `assert_no_packages` + `assert_no_landing` — applied consistently to every mutation test per adversarial reviewer requirement.
- Symlink tests assert exact count (2 = missing + loose) rather than bounded range. DirEntry::file_type() does NOT follow symlinks, so behavior is deterministic.

## Key files
- `tests/unit/ts_arch_01/rule_02.rs` — 36 tests

## Next steps
- Rules 03-07 tests (same adversarial pattern)
