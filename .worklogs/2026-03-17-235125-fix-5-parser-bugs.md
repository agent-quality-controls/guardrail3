# Fix 5 parser bugs found by adversarial tests

**Date:** 2026-03-17 23:51
**Scope:** diff.rs, generate.rs, clippy.rs, deny.rs, adversarial_generate.rs

## Bugs fixed
1. Multiline entries — join continuation lines until } found
2. No-space syntax — normalize whitespace before matching {path=
3. Cross-section dedup — prefix entries with section name (methods:/types:)
4. Override duplicates base — deduplicated_override() skips existing entries
5. Malformed override — validate_override_content() skips invalid lines with warning
6. Comment-only override — empty after validation, no header injected
