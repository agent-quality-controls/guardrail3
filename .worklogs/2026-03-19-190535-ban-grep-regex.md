# Ban regex/grep — enforce structured parsing only

**Date:** 2026-03-19 19:05
**Scope:** deny.toml, CLAUDE.md

## Summary
Banned regex crate and all alternatives via deny.toml. Updated CLAUDE.md principle 7 to mandate structured parsers for every format. Added Don'ts 13 and 14.

## Changes
- deny.toml: banned `regex` (wrapper: tree-sitter, globset, ignore), `fancy-regex`, `onig`, `pcre2`
- CLAUDE.md principle 7: expanded from "AST-based scanning" to "Structured parsing only" with parser-per-format table
- CLAUDE.md Don'ts 13-14: explicit ban on regex/grep/contains on config files
