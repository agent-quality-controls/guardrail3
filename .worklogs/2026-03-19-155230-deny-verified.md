# Deny.toml resolution empirically verified

**Date:** 2026-03-19 15:52
**Scope:** coverage/deny.rs, plans/by_file/rs/deny-toml.md

## Summary
Empirically verified deny.toml resolution: walks up from CWD, crosses workspace boundaries, can be shadowed by intermediate files. Identical to clippy. Corrected plan file (previously claimed "does NOT walk up"). Updated resolution description string.

## Verified facts
1. Walk-up from CWD (must have Cargo.toml)
2. Walks past workspace root to project root
3. Intermediate deny.toml shadows workspace root for dirs below it
4. Priority: deny.toml > .deny.toml > .cargo/deny.toml
5. CWD must have Cargo.toml (errors otherwise)
