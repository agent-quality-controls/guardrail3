# ESLint v10 resolution empirically verified

**Date:** 2026-03-19 16:07
**Scope:** coverage/eslint.rs

## Verified facts (tested on steady-parent with ESLint 10.0.3)
1. Walk-up per file — nearest eslint.config.* wins, complete replacement
2. apps/landing/ local config shadows root: no strict-boolean-expressions for landing files
3. apps/admin/ no local config: uses root, has strict-boolean-expressions
4. packages/metadata/ no local config: uses root
5. Intermediate apps/eslint.config.mjs shadows everything under apps/ but NOT packages/
6. Config without matching files pattern returns undefined for non-matching files
7. Same walk-up + shadow behavior as all 4 Rust tools
