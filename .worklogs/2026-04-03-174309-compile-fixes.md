# Compile fixes for libarch orphan removal

Fixed compilation errors from removing library profile guards and disabling CODE-26/27.

## Changes
- DEPS-08: Removed `profile_name` field from `AllowlistCoverageFacts` struct and construction site
- DEPS-08 test helper: Suppressed unused `profile_name` parameter
- CODE inputs: Added `#[allow(dead_code)]` on `profile_name` field (still populated but no rule reads it)
- CODE parse: Added `#[allow(dead_code)]` on functions/types used only by removed CODE-26/27
- ARCH-05: Changed "project root" → "the root workspace" in boundary label
