# Summary

Fixed broken L40-L70 G3RS behavior fixtures so they validate the fixture workspace instead of a nonexistent nested `repo` path. Closed L40-L80 Error/Warn outputs in the behavior manifest and fixed baseline normalization for volatile Cargo output.

# Decisions Made

- Changed L40-L70 fixture commands to `g3rs validate --path . --inventory` because `run_from = "repo"` already sets the cwd to the fixture repo.
- Implemented `runner_mode = "path_without_delegated_tools"` in the behavior runner so L50 actually hides delegated tools while preserving the Rust toolchain.
- Added complete workspace lint tables to L40-L70 fixtures so deeper layer fixtures are not polluted by unrelated Cargo lint-table failures.
- Removed irrelevant cargo-deny unused allowlist warnings from L50-L70 by setting `unused-allowed-license = "allow"` and `unused-allowed-source = "allow"`.
- Fixed normalizer regexes for Cargo timing, test binary hashes, and macOS `/private` temp paths.
- Extended baseline verification so L40-L80 Error/Warn rows are closed against manifest `required_results`.

# Key Files

- `.plans/2026-05-13-020115-fix-g3rs-l40-l70-fixture-paths.md`
- `.plans/2026-05-13-020115-fix-g3rs-l40-l70-fixture-paths.md.manifest.toml`
- `.plans/2026-05-12-183156-guardrail3-behavior-replay-fixture-migration.md.manifest.toml`
- `scripts/behavior/baseline_common.py`
- `scripts/behavior/verify-baselines.py`
- `behavior/fixtures/g3rs/L40-required-inputs-present-invalid`
- `behavior/fixtures/g3rs/L50-required-inputs-valid-delegated-tools-missing`
- `behavior/fixtures/g3rs/L60-delegated-tools-present-policy-invalid`
- `behavior/fixtures/g3rs/L70-delegated-policy-valid-project-policy-violated`

# Verification

- `scripts/behavior/verify-all.sh`
- `python3 -m py_compile scripts/behavior/*.py`
- `git diff --check`
- `g3rs validate --path apps/guardrail3-rs`
- `g3rs validate-repo`

# Next Steps

- Continue higher-layer fixture migration with closed Error/Warn manifests from the start.
