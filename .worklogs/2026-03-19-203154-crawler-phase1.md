# Wire crawler into validate — Phase 1: plumbing

**Date:** 2026-03-19 20:31
**Scope:** validate.rs, rs/validate/mod.rs, ts/validate/mod.rs, hooks/validate.rs, main.rs

## Summary
Called `crawl()` once at the top of validate, threaded `&CrawlResult` through all three orchestrators (rs, ts, hooks). No check logic changed — parameters prefixed with `_crawl` until Phase 2 uses them.
