# Rule 06 /test-attack — 35 tests, all issues fixed

**Date:** 2026-03-20 22:43

## Summary
/test-attack found 2 CHECK BUGS + 10 missing scenarios + 21 assertion gaps + OUTER_CONTAINERS gap. All test issues fixed (check bugs left unfixed — tests document them).

## CHECK BUGS (in code, NOT fixed — tests prove they exist)
1. .gitkeep-only leaf rejected — subdir_with_only_gitkeep test expects the error
2. Empty crates/ treated as absent — crates_dir_exists_but_empty test documents it
3. Dangling symlink in crates/ makes has_crates=true — dangling_symlink_in_crates_dir documents it

## Test fixes
- OUTER_CONTAINERS: added ports/inbound (6th entry), count 19→21
- All assertions on &r6 not &errors
- is_empty→assert_eq(0) everywhere
- Total r6 count pins on all tests
- assert_no_ts_apps + assert_no_packages on all tests

## 10 new tests
missing_container_early_return, malformed_cargo_toml_still_valid, hidden_dir_as_leaf,
cargo_toml_wrong_case, nested_garbage_no_recursion, dangling_symlink_in_crates_dir,
maximally_complex_single_container, unicode_lookalike_subdir, cargo_toml_is_directory,
subdirs_plus_loose_alongside_valid

258 passing, 5 intentional failures (rules 07-11).
