# Steps 02-03: Capture before results + add syn helpers

**Date:** 2026-03-15 22:46
**Scope:** Adversarial test baseline + AST parsing module

## Step 02 findings
Grep scanner better than expected — only 3 genuine false positives out of 40 fixtures:
1. multiline_string.rs: R32 false positive (line continuation fools scanner)
2. string_unwrap.rs: R44 false positive (.unwrap() in string literal)
3. cfg_gated_use.rs: R58 debatable (cfg-gated std::fs import)

## Step 03 deliverables
10 syn-based AST helpers with 22 unit tests. Each helper correctly ignores patterns in strings/comments. 280 total tests passing.
