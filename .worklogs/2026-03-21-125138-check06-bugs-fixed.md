# Fix 2 check_06 bugs found by /test-attack

**Date:** 2026-03-21 12:51

## Bugs fixed in check_06_leaf_valid.rs

1. **.gitkeep-only leaf accepted as valid placeholder** — added has_gitkeep check before emitting "missing Cargo.toml" error. Subdirs with only .gitkeep are now valid placeholders reserving a name for a future crate.

2. **Empty crates/ detected via metadata** — replaced `!fs.list_dir(crates/).is_empty()` with `fs.metadata(crates/).is_some_and(|m| m.is_dir())`. Empty crates/ is now correctly detected as hex-in-hex scaffold. Recursion into inner structural checks produces the right errors (missing domain/app/ports/adapters).

## Tests updated
- subdir_with_only_gitkeep: now expects 0 errors (was 1)
- crates_dir_exists_but_empty: now expects 0 rule6 errors + inner structural errors
- maximally_complex_single_container: gitkeep_only_sub no longer fires, count 3→2

## Bug NOT fixed (skipped)
- Dangling symlink in crates/ makes has_crates=true — accepted as-is

258 passing, 5 intentional failures.
