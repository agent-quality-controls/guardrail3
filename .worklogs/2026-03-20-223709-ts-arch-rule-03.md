# TS-ARCH-01 Rule 03 Tests

## Summary
43 tests for TS hex arch rule 03 (adapters/ and ports/ must contain exactly {inbound, outbound}). Written at converged quality from rule_02 patterns, then hardened via adversarial round.

## Decisions
- `rule3_errors` filter isolates rule 03 from other T-ARCH-01 sub-rules by matching titles containing adapters/ or ports/ paths. Excludes inbound/outbound subdirectory errors (rule 04+).
- `assert_standard` helper applied consistently to all mutation tests.
- Added early-return guard tests: when adapters/ or ports/ is missing entirely, rule 03 should produce 0 errors (rule 02 handles the missing parent).
- Added edge cases from adversarial review: wrong case, .gitignore/.DS_Store as loose, hidden dirs, file-coexists, .gitkeep-as-dir, nested unexpected, unicode lookalike.

## Key files
- `tests/unit/ts_arch_01/rule_03.rs` — 43 tests

## Next steps
- Rules 04-07 tests
