# Rule 03 exhaustive tests — 25 tests

**Date:** 2026-03-20 18:11

## Summary
25 tests for Rule 3 (adapters/ and ports/ must contain exactly {inbound, outbound}). Covers missing dirs, unexpected dirs, loose files, combinations, edge cases, inner hex isolation, label prefix, idempotency, packages/TS exclusion.

Key discovery: check_03's early return on empty dir means removing both inbound+outbound produces 0 Rule 3 errors — Rule 2 handles it instead.
