# Final hardening — fix every remaining issue, pass own guardrails

**Date:** 2026-03-16 12:00
**Task:** Fix all adversarial findings, split oversized files, fix clippy, update goldens, self-validate

## Issues to fix

### HIGH
1. `is_cfg_test_attr` only matches bare `#[cfg(test)]` — must handle `cfg(all(test, ...))`, `cfg(any(test, ...))` etc.

### MEDIUM
2. `#[allow]` on expressions (let bindings, match arms, loop bodies) invisible — add visit_local and visit_arm to ItemAllowVisitor
3. `std::fs::File::open()` not caught by InlineStdFsVisitor — it's a type method call via std::fs, should be caught
4. R53 only checks `[workspace.lints.rust]` — add `[lints.rust]` for single-crate projects
5. R-GARDE-05 AST path miscounts split `#[derive(Deserialize)] #[derive(Validate)]` — must check across all derives on same item
6. `#[ignore = "reason"]` flagged as violation by R-TEST-07 — must accept attribute-based reasons
7. R43 AST drops `std::todo!()` / `core::todo!()` — match must handle path-qualified names
8. T34/T35 `check_comment_pattern` matches inside string literals — need tree-sitter or at minimum skip quoted content

### STRUCTURAL
9. Split ast_helpers.rs (883 lines) under 500
10. Split ts/ast_helpers.rs (686 lines) under 500
11. Fix 18 clippy errors in strip_string_literals

### FINAL
12. Update golden snapshots
13. Run guardrail3 validate . — must pass with 0 errors
