# Expand garde enforcement — all input boundary derives

**Date:** 2026-03-16 17:19
**Task:** R-GARDE-01 becomes error. R-GARDE-05 checks Parser/Args/FromRow too.

## Changes
1. R-GARDE-01: garde missing → Error (not info). Every project must have garde.
2. R-GARDE-05: expand from just Deserialize to Deserialize + Parser + Args + FromRow.
   Any struct with these derives that doesn't also have Validate → Error.
3. Update find_derive_attributes in ast_helpers to detect all four derives.
4. Add garde to guardrail3's own Cargo.toml and derive Validate on CLI structs.
5. Update help + guide with expanded check descriptions.
