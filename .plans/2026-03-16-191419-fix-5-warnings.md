# Fix 5 validation warnings

**Date:** 2026-03-16 19:14
**Task:** Fix 5 guardrail3 warnings: R-ARCH-01 path resolution, mutants.toml, README, release-plz.toml, cargo-semver-checks

## Goal
Resolve all 5 warnings so guardrail3 validate passes cleanly.

## Approach

### 1. R-ARCH-01 path resolution bug
- Add `project: &ProjectInfo` parameter to `check_hex_arch_structure`
- Match crate name against `project.workspace_member_dirs` / `project.workspace_members` to find the actual directory
- Update call site in `mod.rs` to pass `project`
- Update test call sites

### 2. R-TEST-02: Create mutants.toml
- Create `apps/guardrail3/.cargo/mutants.toml` with timeout_multiplier = 2.0

### 3. R-PUB-04: Create README.md
- Create `apps/guardrail3/README.md` with brief description

### 4. R-REL-03: Fix release-plz.toml
- Add `[[package]]` entry for guardrail3

### 5. R-REL-08: Install cargo-semver-checks
- Run `cargo install cargo-semver-checks`

## Files to Modify
- `apps/guardrail3/src/app/rs/validate/hex_arch_checks.rs` — fix path resolution
- `apps/guardrail3/src/app/rs/validate/mod.rs` — pass project to check_hex_arch_structure
- `apps/guardrail3/tests/unit/test_hex_arch_checks.rs` — update test calls
- `apps/guardrail3/.cargo/mutants.toml` — create
- `apps/guardrail3/README.md` — create
- `release-plz.toml` — add package entry
