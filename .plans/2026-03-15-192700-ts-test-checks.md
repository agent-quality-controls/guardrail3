# Add TypeScript test quality checks (T-TEST-01 through T-TEST-05)

**Date:** 2026-03-15 19:27
**Task:** Create src/ts/validate/test_checks.rs with 5 test quality checks + tests

## Goal
New file with 5 checks scanning TS projects for test quality signals: Stryker config, test file existence, test runner config, .skip() without reason, .only() in committed code.

## Approach

### Step-by-step plan
1. Create `src/ts/validate/test_checks.rs` with all 5 checks
2. Wire it into `src/ts/validate/mod.rs` as a new section
3. Follow patterns from `source_scan.rs` for file walking and CheckResult creation

### Key decisions
- **Helper functions for source scanning checks:** T-TEST-04 and T-TEST-05 get helper fns taking `&str` content + `&str` filename so tests can pass content directly without filesystem
- **Walking for test files (T-TEST-02):** Use `walkdir::WalkDir` skipping node_modules, same pattern as `collect_ts_files` in source_scan.rs

## Files to Modify
- `src/ts/validate/test_checks.rs` — NEW: all 5 checks + 10 tests
- `src/ts/validate/mod.rs` — wire in test_checks section
