# Migrate R34-R37 from grep to syn

**Date:** 2026-03-16 08:31
**Task:** Rewrite check_garde_skip (R34-R35) and check_cfg_attr_allow (R37) to use ast_helpers

## Goal
R34, R35, R37 use syn-based AST analysis instead of grep. R36 stays grep-based (comment scanning). Grep fallback on parse failure.

## Approach
1. Add `use super::ast_helpers;` import
2. Rewrite `check_garde_skip` — parse file, use `find_garde_skips()` for lines, check raw content for `//` comment
3. Rewrite `check_cfg_attr_allow` — parse file, use `find_cfg_attr_allows()` for lines+lints, emit R37
4. Both fall back to existing grep logic on parse failure
5. Remove `#[allow(clippy::string_slice)]` from both functions (no longer needed)

## Files to Modify
- `src/rs/validate/allow_checks.rs` — rewrite check_garde_skip and check_cfg_attr_allow
