# Implement hooks validation and deployment checks (H1-H11, D1-D5)

**Date:** 2026-03-15 13:00
**Task:** Replace stub in src/hooks/validate.rs with full hook and deployment validation

## Goal
Hook validation returns a Report with two sections ("Hook checks", "Deployment checks") containing H1-H11 and D1-D5 checks. Wire into commands/validate.rs to merge into combined report.

## Approach

### Step-by-step plan
1. Rewrite `src/hooks/validate.rs` — change signature to `run(path: &Path, has_rust: bool, has_typescript: bool) -> Report`, implement all H1-H11 and D1-D5 checks
2. Update `src/hooks/mod.rs` if needed (just re-exports validate)
3. Update `src/commands/validate.rs` — replace println stub with actual call to hooks::validate::run, merge sections into combined_report
4. Update `src/main.rs` — pass args through properly for hooks validate command (needs discover + merge)
5. Verify `cargo build` succeeds

### Key decisions
- Pass has_rust/has_typescript as bools rather than full ProjectInfo to keep hooks module loosely coupled
- Use std::process::Command for `git config` and `which` checks
- Use `#[cfg(unix)]` for permission checks (mode bits)

## Files to Modify
- `src/hooks/validate.rs` — full rewrite
- `src/commands/validate.rs` — wire hooks into combined report
- `src/main.rs` — update hooks command to use proper validation
