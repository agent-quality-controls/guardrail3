# Fix R50 anyhow ban, R58 false positives, add LICENSE files

**Date:** 2026-03-15 20:18
**Task:** Fix 3 pre-existing issues: add LICENSE files, fix R50 anyhow ban, fix R58 false positives in test code

## Goal
- LICENSE-MIT and LICENSE-APACHE files exist at repo root
- anyhow is skipped in deny.toml so R50 doesn't flag it
- R58 check skips `std::fs` usage inside `#[cfg(test)]` blocks

## Approach

### Step-by-step plan
1. Create `LICENSE-MIT` with standard MIT text, copyright "guardrail3 contributors" 2024-2026
2. Create `LICENSE-APACHE` with standard Apache 2.0 text
3. Edit `deny.toml` — add anyhow to the skip list following existing format
4. Edit `src/rs/validate/code_quality_checks.rs` — modify `check_direct_fs_usage` to track `#[cfg(test)]` brace depth and skip lines inside test blocks
5. Add a regression test for the R58 fix
6. Run cargo build, cargo test, validate

## Files to Modify
- `deny.toml` — add anyhow skip entry
- `src/rs/validate/code_quality_checks.rs` — fix R58 to skip test blocks, add test
- `LICENSE-MIT` — new file
- `LICENSE-APACHE` — new file
