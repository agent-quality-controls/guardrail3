# Rewrite R-ARCH-01: Auto-detect hex arch structure

**Date:** 2026-03-20 09:53
**Task:** Rewrite `check_hex_arch_structure` to auto-detect service apps from `apps/` directory layout

## Goal
R-ARCH-01 should scan `apps/` directory, find all subdirectories with `Cargo.toml`, and validate each has proper hex arch structure (`crates/{adapters,app,domain,ports}` with proper sub-structure).

## Approach

### Step-by-step plan
1. Add `list_dir_names` helper that wraps `fs.list_dir()` to return `(Vec<String>, Vec<String>)` of (dir_names, file_names) excluding `.gitkeep`
2. Rewrite `check_hex_arch_structure` with new signature (no `project`/`cfgs` params)
3. Auto-detect service apps via `fs.list_dir(root.join("apps"))`
4. For each service app, validate the 5-level structure per spec
5. Update call site in `mod.rs` to use new signature
6. Build and test

### Key decisions
- Use `fs.list_dir()` which returns empty vec for non-existent dirs
- To check if a specific dir exists within a parent, list the parent and check entries
- Use `BTreeSet` for expected directory sets

## Files to Modify
- `apps/guardrail3/src/app/rs/validate/hex_arch_checks.rs` — rewrite R-ARCH-01 function + add helper
- `apps/guardrail3/src/app/rs/validate/mod.rs` — update call site
