# Fix 6 clippy errors

**Date:** 2026-03-19 12:04
**Task:** Fix 6 clippy errors across 3 files

## Goal
Zero clippy errors in the project.

## Approach

1. **jscpd.rs:41** — Replace closure `|t| t.as_u64()` with method reference `serde_json::Value::as_u64` in `and_then()`.
2. **rust_toolchain.rs:38** — Replace `.map(|s| s.trim().trim_matches('"')).unwrap_or("unknown")` with `.map_or("unknown", |s| s.trim().trim_matches('"'))`.
3-6. **map.rs:249,254,269,280** — Swap if/else branches to use positive `is_empty()` check first (clippy::if_not_else).

## Files to Modify
- `apps/guardrail3/src/commands/coverage/jscpd.rs` — method reference
- `apps/guardrail3/src/commands/coverage/rust_toolchain.rs` — map_or
- `apps/guardrail3/src/commands/map.rs` — swap 4 if/else blocks
