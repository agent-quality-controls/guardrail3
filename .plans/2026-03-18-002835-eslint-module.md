# Add eslint.rs module for building eslint.config.mjs

**Date:** 2026-03-18 00:28
**Task:** Create `apps/guardrail3/src/domain/modules/eslint.rs` that builds a complete eslint.config.mjs string.

## Goal
A new Rust module that generates a complete, syntactically valid ESLint flat config (eslint.config.mjs) with conditional sections for content-app and service-app features.

## Approach
- Use const &str slices for all static sections to keep the file under 500 lines
- Use `String::push_str` for assembly, with conditional blocks for has_content_app and has_service_app
- Follow the clippy.rs pattern of const sections + builder function
- No unwrap, no unsafe, no HashMap

## Files to Modify
- `apps/guardrail3/src/domain/modules/eslint.rs` — new file, the eslint config builder
