# Checker Architecture — Deploy Checkers (5 rules)

**Date:** 2026-03-21
**Parent plan:** `2026-03-21-153251-checker-architecture.md`

## File Structure

```
crates/app/checkers/deploy/
├── mod.rs                              # orchestrator
│
└── ts/                                 # 5 rules — TS deployment checks
    ├── mod.rs                          # check(), reads multiple deploy config files
    ├── rule_01_railpack_exists.rs      # DEPLOY-TS-01 (old D1): railpack.json exists
    ├── rule_02_railpack_config.rs      # DEPLOY-TS-02 (old D2): railpack config correctness
    ├── rule_03_standalone.rs           # DEPLOY-TS-03 (old D3): Next.js output: "standalone"
    ├── rule_04_tracing_root.rs         # DEPLOY-TS-04 (old D4): experimental.outputFileTracingRoot
    └── rule_05_tailwind.rs             # DEPLOY-TS-05 (old D5): tailwind in deps not devDeps
```

## Rule count: 5 rules in 1 checker

| Checker | Rules | Input |
|---------|-------|-------|
| deploy/ts | 5 | railpack.json, next.config.js, package.json |

Currently TS-only. If Rust deploy checks are added later (e.g., Dockerfile, cross-compilation), they'd go under `deploy/rs/`.
