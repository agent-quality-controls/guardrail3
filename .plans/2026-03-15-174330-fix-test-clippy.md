# Fix all clippy errors in test code

**Date:** 2026-03-15 17:43
**Task:** Fix all clippy errors from `cargo clippy --all-targets`

## Goal
Zero clippy errors on `--all-targets`, all 43 tests pass, `cargo fmt --check` clean.

## Approach

### Files needing `#![allow]` in test modules
1. `src/discover.rs` — needs disallowed_methods, expect_used, unwrap_used
2. `src/rs/validate/deny_inventory.rs` — needs indexing_slicing, expect_used, needless_raw_string_hashes fix
3. `src/rs/validate/source_scan.rs` — needs indexing_slicing
4. `src/rs/validate/structure_checks.rs` — needs indexing_slicing
5. `src/rs/validate/allow_checks.rs` — needs indexing_slicing
6. `src/rs/validate/code_quality_checks.rs` — needs indexing_slicing
7. `src/report/types.rs` — no clippy errors (no indexing used)

### Structural fixes
- `src/discover.rs` — move `detect_typescript` fn above the test module (items_after_test_module)
- `src/rs/validate/deny_inventory.rs` — change `r#"..."#` to `r"..."` (needless_raw_string_hashes)
