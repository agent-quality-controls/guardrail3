# Coverage maps — one file per config file type

**Date:** 2026-03-18 19:00
**Task:** Split coverage map into one file per config type. Research actual tool resolution for each. Fix clippy map to show proper tree.

## Files to create

Under `src/commands/coverage/`:
- `mod.rs` — dispatcher
- `clippy.rs` — clippy.toml coverage (walk-up from CARGO_MANIFEST_DIR)
- `deny.rs` — deny.toml coverage (CWD only, no walk-up)
- `rustfmt.rs` — rustfmt.toml coverage (walk-up from file)
- `eslint.rs` — eslint.config.* coverage (CWD only for stable, walk-up for experimental)
- `tsconfig.rs` — tsconfig.json coverage (extends chain)
- `stylelint.rs` — .stylelintrc.* coverage (cosmiconfig walk-up)
- `npmrc.rs` — .npmrc coverage (project root only)
- `cspell.rs` — cspell config coverage (walk-up from file)

Each file documents the actual tool resolution rules in comments.
Each takes CrawlResult and produces tree output.

## Approach
- Delete `coverage_map.rs` (monolith)
- Create `src/commands/coverage/` module directory
- Fix the tree display to be hierarchical (workspaces contain members)
