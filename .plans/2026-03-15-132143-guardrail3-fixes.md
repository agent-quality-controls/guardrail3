# Fix guardrail3: rs validate, ts generate, profiles, T60-T61

**Date:** 2026-03-15 13:21
**Task:** Fix 4 issues in guardrail3

## Goal
1. `rs validate` only runs Rust checks (not full validate)
2. `ts generate` produces .npmrc, tsconfig.base.json, .jscpd.json, eslint.config.mjs
3. Profile differentiation for clippy/deny (library, minimal, monorepo)
4. T60-T61 checks in TS validate

## Approach

### 1. Fix rs validate (main.rs)
- Change `RsCommands::Validate` to call rs::validate::run directly (like ts validate does)
- Use discover, resolve_scoped_files_pub, format, exit code

### 2. Implement ts generate (generate.rs + canonical.rs)
- Add NPMRC, TSCONFIG_BASE, JSCPD, ESLINT_STARTER constants to canonical.rs
- Wire run_ts to generate files when canonical config enabled
- Add to generate_all_files for monorepo profile

### 3. Profile differentiation (clippy.rs, deny.rs, generate.rs)
- Add library/minimal profile functions to clippy.rs
- Add library/minimal ban entries to deny.rs
- Wire generate to select based on profile name
- Library: always pure, add IO bans, no feature bans
- Minimal: subset methods/types, no bans, no feature bans

### 4. T60-T61 (config_files.rs)
- T60: content import restriction in eslint config
- T61: velite config exists check

## Files to Modify
- `src/main.rs` — fix rs validate dispatch
- `src/modules/canonical.rs` — add TS canonical content
- `src/modules/clippy.rs` — add profile functions
- `src/modules/deny.rs` — add profile functions + library IO bans
- `src/modules/mod.rs` — register new modules
- `src/commands/generate.rs` — wire ts generate + profile selection
- `src/ts/validate/config_files.rs` — add T60-T61
