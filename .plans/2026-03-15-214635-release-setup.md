# Set up guardrail3 for crates.io release and binary distribution

**Date:** 2026-03-15 21:46
**Task:** Tasks #63-#68: Update Cargo.toml publish metadata, add binstall metadata, update release-plz.toml, create CI/release/binary-release GitHub Actions workflows.

## Goal
guardrail3 is ready for crates.io publishing and automated binary distribution via GitHub releases.

## Approach

### Step-by-step plan
1. **Task #63:** Update Cargo.toml [package] — remove `publish = false`, add repository/readme/keywords/categories
2. **Task #64:** Add `[package.metadata.binstall]` section at end of Cargo.toml
3. **Task #65:** Update release-plz.toml — replace template placeholder with actual package name
4. **Task #66:** Create `.github/workflows/release.yml`
5. **Task #67:** Create `.github/workflows/binary-release.yml`
6. **Task #68:** Create `.github/workflows/ci.yml`
7. Run `cargo build` to verify Cargo.toml compiles
8. Run `cargo run -- validate --release .` to check warnings

## Files to Modify
- `Cargo.toml` — publish metadata + binstall metadata
- `release-plz.toml` — replace template with actual config
- `.github/workflows/release.yml` — new file
- `.github/workflows/binary-release.yml` — new file
- `.github/workflows/ci.yml` — new file
