# Migrate R40-R44 from grep to syn AST

**Date:** 2026-03-16 08:31
**Task:** Migrate R42 (unsafe), R43 (todo/unimplemented), R44 (unwrap/expect), R40-R41 (use count) from grep-based to syn AST-based checks with grep fallback.

## Goal
Replace grep-based pattern matching with syn AST visitors for R40-R44. Eliminates false positives from strings, comments, and identifiers that happen to contain keywords.

## Approach

### structure_checks.rs
1. Add `use super::ast_helpers;` import
2. Rewrite `check_use_count` to try `ast_helpers::parse_file` + `count_use_statements`, fall back to grep
3. Rewrite `check_unsafe` to try `ast_helpers::parse_file` + `find_unsafe_usage`, fall back to grep

### code_quality_checks.rs
1. Add `use super::ast_helpers;` import
2. Rewrite `check_todo_macros` to try `ast_helpers::parse_file` + `find_forbidden_macros`, fall back to grep. Note: `find_forbidden_macros` also finds `panic!` but current R43 only flags `todo!`, `unimplemented!`, `unreachable!`. Need to filter appropriately. Actually `unreachable!` is not in `find_forbidden_macros` — it finds `todo`, `unimplemented`, `panic`. The current grep checks `unreachable!` as Info. For AST path: use `find_forbidden_macros` for todo/unimplemented (as Warn), but unreachable! is NOT in the visitor. Need to handle unreachable separately or keep grep for it. Looking at the visitor: it matches "todo" | "unimplemented" | "panic". We need todo/unimplemented as Warn, unreachable as Info. The visitor doesn't cover unreachable. Options: (a) add unreachable to visitor, (b) keep grep for unreachable only. Better: add unreachable to the visitor list, then filter in the check function.
3. Rewrite `check_unwrap_expect` to try `ast_helpers::parse_file` + `find_unwrap_expect`, fall back to grep

### ast_helpers.rs
1. Add `unreachable` to `ForbiddenMacroVisitor` match list so R43 can use it

## Files to Modify
- `src/rs/validate/ast_helpers.rs` — add "unreachable" to ForbiddenMacroVisitor
- `src/rs/validate/structure_checks.rs` — rewrite check_use_count and check_unsafe
- `src/rs/validate/code_quality_checks.rs` — rewrite check_todo_macros and check_unwrap_expect
