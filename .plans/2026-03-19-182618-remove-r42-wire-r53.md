# Remove dead check_unsafe (R42) and wire check_unsafe_code_forbid (R53)

**Date:** 2026-03-19 18:26
**Task:** Fix two issues in Rust source scan

## Goal
1. Remove `check_unsafe` (R42) — redundant with clippy's `unsafe_code = "forbid"`
2. Wire `check_unsafe_code_forbid` (R53) into the source_scan orchestrator

## Approach

### Step 1: Remove check_unsafe from structure_checks.rs
Delete lines 71-93 (the R42 function). Keep check_unsafe_code_forbid (R53).

### Step 2: Remove check_unsafe call from fuzz target
Remove line 44 in fuzz/fuzz_targets/fuzz_source_scan.rs.

### Step 3: Remove check_unsafe test from test file
Remove R42 test (lines 89-100) and the import in rs_structure_checks_test.rs.

### Step 4: Wire check_unsafe_code_forbid into source_scan.rs
Add it in the workspace-level section (after line 69), since R53 checks workspace Cargo.toml once.
Signature: `check_unsafe_code_forbid(fs, workspace_root, &mut results)` — already has the right params available.

### Step 5: Compile check

## Files to Modify
- `apps/guardrail3/src/app/rs/validate/structure_checks.rs` — remove check_unsafe
- `apps/guardrail3/src/app/rs/validate/source_scan.rs` — wire check_unsafe_code_forbid
- `apps/guardrail3/tests/unit/rs_structure_checks_test.rs` — remove R42 test + import
- `fuzz/fuzz_targets/fuzz_source_scan.rs` — remove check_unsafe call
