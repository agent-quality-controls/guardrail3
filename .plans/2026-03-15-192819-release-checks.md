# Implement release_checks.rs — 23 checks + 45 tests

**Date:** 2026-03-15 19:28
**Task:** Create src/rs/validate/release_checks.rs with R-PUB, R-REL, R-BIN checks

## Goal
New validation module for release readiness: per-crate publish metadata, repo-level release config, binary distribution checks.

## Approach
- Split into two files to stay under 500 lines: release_checks.rs (orchestrator + per-crate R-PUB) and release_repo_checks.rs (R-REL + R-BIN + all tests for repo/binary)
- Follow existing patterns from dependency_scan.rs and config_files.rs
- Use crate::fs for all filesystem, walkdir for recursive discovery
- semver parsing done manually (X.Y.Z with optional -prerelease) since no semver crate in deps
- Tests use inline TOML parsing helpers

## Files to Create
- `src/rs/validate/release_checks.rs` — orchestrator + crate discovery + R-PUB-01 through R-PUB-12 checks + tests
- `src/rs/validate/release_repo_checks.rs` — R-REL-01 through R-REL-08 + R-BIN-01 through R-BIN-03 checks + tests
