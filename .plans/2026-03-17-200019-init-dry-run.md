# Add --dry-run to init commands

**Date:** 2026-03-17 20:00
**Task:** Add --dry-run flag to rs init and ts init that shows what would change without writing

## Approach
- Add `--dry-run` bool flag to init CLI args
- When dry-run: compute what would be written, diff against existing, print diff
- Don't write any files
- Works with and without --force

## Files
- cli.rs — add dry_run flag to RsCommands::Init and TsCommands::Init
- commands/init.rs — add dry-run logic
- main.rs — pass flag through
