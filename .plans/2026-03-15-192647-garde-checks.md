# Implement Garde boundary validation checks (R-GARDE-01 through R-GARDE-05)

**Date:** 2026-03-15 19:26
**Task:** Create src/rs/validate/garde_checks.rs with 5 Garde boundary validation checks and 11 tests.

## Goal
New file with 5 checks that validate Garde adoption in a Rust workspace: dependency presence, clippy method/type bans for serde/axum/reqwest, and a struct inventory scan.

## Approach

### Step-by-step plan
1. Create `src/rs/validate/garde_checks.rs` with:
   - `check(workspace_root: &Path) -> Vec<CheckResult>` orchestrator
   - `check_garde_dependency(content: &str) -> Vec<CheckResult>` for R-GARDE-01
   - `check_serde_method_bans(content: &str, file: &str) -> Vec<CheckResult>` for R-GARDE-02
   - `check_axum_type_bans(content: &str, file: &str) -> Vec<CheckResult>` for R-GARDE-03
   - `check_reqwest_json_ban(content: &str, file: &str) -> Vec<CheckResult>` for R-GARDE-04
   - `check_derive_inventory(rs_files: &[String]) -> Vec<CheckResult>` for R-GARDE-05
2. Parse clippy.toml similar to clippy_coverage.rs (extract paths from disallowed-methods/types)
3. Parse workspace Cargo.toml for garde dependency
4. Walk .rs files with walkdir for derive inventory scan
5. Full test suite: 11 tests covering all checks

### Key decisions
- **Testable inner functions:** Orchestrator reads files and delegates to inner functions that accept &str content. Tests pass content directly.
- **clippy.toml parsing:** Reuse same pattern as clippy_coverage.rs (toml::Value, get array, extract path fields)
- **File walking for R-GARDE-05:** Use walkdir same as source_scan.rs, reuse collect pattern

## Files to Modify
- `src/rs/validate/garde_checks.rs` (NEW) - all 5 checks + tests
