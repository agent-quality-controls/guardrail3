# Fix 9 guardrail3 issues

**Date:** 2026-03-15 12:32
**Task:** Apply 9 bug fixes across report and validation modules

## Goal
Fix all listed issues so cargo build succeeds and output is correct.

## Approach

### Step-by-step plan
1. **types.rs** — Remove `score()` method and `total_checks()` (only used by score)
2. **text.rs** — Remove score display from `print_summary`, remove unused import
3. **cargo_lints.rs** — Split `check_lint_level` to use R26 for missing, R27 for wrong level; update callers
4. **dependency_scan.rs** — Change R47 cargo-dupes from Error to Warn
5. **source_scan.rs R51** — Add suffix matching for banned dep names (-db, -api, -adapters, -commands)
6. **deny_audit.rs R10** — Change yanked missing from Warn to Error
7. **source_scan.rs filter** — Fix block comment `*/` handling and inline `/* */` stripping
8. **source_scan.rs R30-R35** — Change check_crate_level_allow, check_item_level_allow, check_garde_skip to use filter_non_comment_lines
9. **source_scan.rs R37** — Change unjustified cfg_attr from Warn to Info
10. **source_scan.rs R42** — Remove dead quote_count variable

## Files to Modify
- `src/report/types.rs` — remove score()
- `src/report/text.rs` — remove score display
- `src/rs/validate/cargo_lints.rs` — R26 vs R27 fix
- `src/rs/validate/dependency_scan.rs` — R47 to Warn
- `src/rs/validate/source_scan.rs` — R51 suffix, filter fix, R30-R35 filter usage, R37 Info, R42 cleanup
- `src/rs/validate/deny_audit.rs` — R10 yanked Error
