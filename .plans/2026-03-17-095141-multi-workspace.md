# Multi-workspace auto-discovery

**Date:** 2026-03-17 09:51
**Task:** Find all Cargo.toml with [workspace] in the project tree, scan each

## Current behavior
discover.rs finds ONE workspace at root (or apps/backend/). Stops after first.

## Target behavior
Walk project tree (respecting .gitignore + excluded dirs). Find every Cargo.toml with [workspace]. Each becomes a workspace root with its own members. All members from all workspaces merged into one ProjectInfo.

## Changes
- discover.rs: walk for all workspace Cargo.toml files
- ProjectInfo: cargo_workspace_root becomes Vec<PathBuf>, or workspace_member_dirs accumulates from all workspaces
- mod.rs orchestrator: scan all workspace roots for config checks, merge source scan across all
