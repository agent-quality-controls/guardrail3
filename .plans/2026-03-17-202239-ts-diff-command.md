# Add `guardrail3 ts diff` command

**Date:** 2026-03-17 20:22
**Task:** Add ts diff subcommand and generate_expected_ts function

## Goal
`guardrail3 ts diff` shows what `ts generate` would change without writing files.

## Approach

1. cli.rs: Add `Diff(PathArg)` variant to `TsCommands`
2. main.rs: Add match arm in `handle_ts` calling `commands::diff::run_ts`
3. generate.rs: Add `generate_expected_ts` that returns TS files as Vec<(String, String)> using existing `generate_ts_files` + hook logic from `run_ts`
4. diff.rs: Add `run_ts` function mirroring `run` but calling `generate_expected_ts`

## Files to Modify
- `apps/guardrail3/src/cli.rs` — add Diff variant
- `apps/guardrail3/src/main.rs` — add match arm
- `apps/guardrail3/src/commands/generate.rs` — add generate_expected_ts
- `apps/guardrail3/src/commands/diff.rs` — add run_ts
