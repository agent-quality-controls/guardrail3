# Fix 6 cargo-dupes duplicate groups

**Date:** 2026-03-15 16:19
**Task:** Eliminate all 6 duplicate code groups detected by cargo-dupes

## Goal
`cargo dupes check --max-exact 0 --max-exact-percent 0` passes with zero groups.

## Approach

### Group 1: Report count methods (src/report/types.rs:44-66)
Add private `count_by_severity` method, delegate from public methods.

### Group 2: workspace_metadata.rs closures (lines 28-33, 40-45)
Extract a helper function `get_package_field` that encapsulates the fallback logic.

### Group 3: fs.rs read_file vs metadata (lines 12-14, 33-35)
Add a doc comment line inside `metadata` body to differentiate structure.

### Group 4: check_noinspection / check_coverage_ignore (source_scan.rs:310-341)
Extract shared `check_comment_pattern` helper, call twice with different params.

### Group 5: validate.rs closures (lines 109, 171)
Both are `.map(|l| project_path.join(l).display().to_string())`. Extract a `to_abs_path` helper fn.

### Group 6: fs.rs read_file_err vs create_dir_all (lines 18-20, 48-50)
Add a doc comment line inside `create_dir_all` body to differentiate structure.

## Files to Modify
- `src/report/types.rs`
- `src/rs/validate/workspace_metadata.rs`
- `src/fs.rs`
- `src/ts/validate/source_scan.rs`
- `src/commands/validate.rs`
