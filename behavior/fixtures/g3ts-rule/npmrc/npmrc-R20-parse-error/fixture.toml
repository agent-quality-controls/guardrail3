id = "npmrc-R20-parse-error"
tool = "g3ts"
run_from = "repo"
commands = [
  ["validate", "workspace", "--path", ".", "--family", "npmrc", "--rules-only", "--inventory"],
]
expected_exit = "nonzero"
level = "family_rule_input_failure"

rule_family = "npmrc"
target_rules = [
  "g3ts-npmrc/root-parseable",
]
expected_findings = [
  "g3ts-npmrc/root-parseable",
]
