id = "npmrc-R10-missing-root"
tool = "g3ts"
run_from = "repo"
commands = [
  ["validate", "--path", ".", "--family", "npmrc", "--rules-only", "--inventory"],
]
expected_exit = "nonzero"
level = "family_rule_input_failure"

rule_family = "npmrc"
target_rules = [
  "g3ts-npmrc/root-exists",
]
expected_findings = [
  "g3ts-npmrc/root-exists",
]
