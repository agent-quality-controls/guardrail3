id = "family-enablement-default-on-non-astro"
tool = "g3ts"
run_from = "repo"
commands = [
  ["validate", "workspace", "--path", ".", "--rules-only", "--inventory"],
]
expected_exit = "nonzero"
level = "family_enablement"

rule_family = "family-enablement"
target_rules = ["g3ts-astro-setup"]
expected_findings = ["g3ts-astro-setup"]
