id = "tsconfig-R40-strict-baseline-violations"
tool = "g3ts"
run_from = "repo"
commands = [
  ["validate", "workspace", "--path", ".", "--family", "tsconfig", "--rules-only", "--inventory"],
]
expected_exit = "nonzero"
level = "family_rule_policy"

rule_family = "tsconfig"
target_rules = [
  "g3ts-tsconfig/extends-or-inline",
  "g3ts-tsconfig/strict-baseline"
]
expected_findings = [
  "g3ts-tsconfig/extends-or-inline",
  "g3ts-tsconfig/strict-baseline"
]
