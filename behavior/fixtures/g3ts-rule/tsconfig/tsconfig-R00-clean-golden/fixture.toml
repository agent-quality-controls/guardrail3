id = "tsconfig-R00-clean-golden"
tool = "g3ts"
run_from = "repo"
commands = [
  ["validate", "--path", ".", "--family", "tsconfig", "--rules-only", "--inventory"],
]
expected_exit = "zero"
level = "family_rule_clean_golden"

rule_family = "tsconfig"
target_rules = []
expected_findings = []
