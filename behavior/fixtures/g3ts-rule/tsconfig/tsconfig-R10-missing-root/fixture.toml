id = "tsconfig-R10-missing-root"
tool = "g3ts"
run_from = "repo"
commands = [
  ["validate", "--path", ".", "--family", "tsconfig", "--rules-only", "--inventory"],
]
expected_exit = "nonzero"
level = "family_rule_input_failure"

rule_family = "tsconfig"
target_rules = [
  "g3ts-tsconfig/exists"
]
expected_findings = [
  "g3ts-tsconfig/exists"
]
