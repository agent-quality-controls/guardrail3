id = "astro-state-R00-clean-golden"
tool = "g3ts"
run_from = "repo"
commands = [
  ["validate", "--path", ".", "--family", "astro-state", "--rules-only", "--inventory"],
]
expected_exit = "zero"
level = "family_rule_clean_golden"

rule_family = "astro-state"
target_rules = []
expected_findings = []
