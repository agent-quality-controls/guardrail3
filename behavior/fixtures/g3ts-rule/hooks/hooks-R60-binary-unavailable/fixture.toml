id = "hooks-R60-binary-unavailable"
tool = "g3ts"
run_from = "repo"
commands = [
  ["validate", "workspace", "--path", ".", "--family", "hooks", "--rules-only", "--inventory"],
]
expected_exit = "nonzero"
level = "family_rule_policy"
git_init = true
runner_mode = "path_without_delegated_tools"

rule_family = "hooks"
target_rules = [
  "g3ts-hooks/g3ts-binary-available"
]
expected_findings = [
  "g3ts-hooks/g3ts-binary-available"
]
