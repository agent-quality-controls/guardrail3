id = "typecov-R10-policy-root-missing"
tool = "g3ts"
run_from = "repo"
commands = [
  ["validate", "--path", ".", "--family", "typecov", "--rules-only", "--inventory"],
]
expected_exit = "nonzero"
level = "family_rule_input_failure"

rule_family = "typecov"
target_rules = [
  "g3ts-typecov/policy-configured"
]
expected_findings = [
  "g3ts-typecov/policy-configured"
]
