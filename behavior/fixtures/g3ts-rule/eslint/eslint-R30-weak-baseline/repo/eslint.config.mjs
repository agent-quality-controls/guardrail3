export default {
  ts: {
    plugins: {},
    rules: {},
    languageOptions: { parserOptions: { projectService: false } }
  },
  tsx: {
    plugins: {},
    rules: {},
    languageOptions: { parserOptions: { projectService: false } }
  },
  test: {
    plugins: {},
    rules: {},
    languageOptions: { parserOptions: { projectService: false } }
  },
  js: {
    plugins: {},
    rules: {},
    languageOptions: { parserOptions: { projectService: false } }
  }
};
