id = "eslint-R10-missing-config"
tool = "g3ts"
run_from = "repo"
commands = [
  ["validate", "workspace", "--path", ".", "--family", "eslint", "--rules-only", "--inventory"],
]
expected_exit = "nonzero"
level = "family_rule_input_failure"

rule_family = "eslint"
target_rules = [
  "g3ts-eslint/exists"
]
expected_findings = [
  "g3ts-eslint/exists"
]
