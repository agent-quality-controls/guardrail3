id = "style-R20-invalid-policy-paths"
tool = "g3ts"
run_from = "repo"
commands = [
  ["validate", "--path", ".", "--family", "style", "--rules-only", "--inventory"],
]
expected_exit = "nonzero"
level = "family_rule_policy"

rule_family = "style"
target_rules = [
  "g3ts-style/policy-paths-valid"
]
expected_findings = [
  "g3ts-style/policy-paths-valid"
]
