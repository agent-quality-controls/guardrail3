id = "fmt-R30-validate-not-wired"
tool = "g3ts"
run_from = "repo"
commands = [
  ["validate", "workspace", "--path", ".", "--family", "fmt", "--rules-only", "--inventory"],
]
expected_exit = "nonzero"
level = "family_rule_policy"

rule_family = "fmt"
target_rules = [
  "g3ts-fmt/validate-runs-format-check"
]
expected_findings = [
  "g3ts-fmt/validate-runs-format-check"
]
