id = "arch-R20-root-parse-error"
tool = "g3ts"
run_from = "repo"
commands = [
  ["validate", "--path", ".", "--family", "arch", "--rules-only", "--inventory"],
]
expected_exit = "nonzero"
level = "family_rule_input_failure"

rule_family = "arch"
target_rules = [
  "g3ts-arch/root-manifest-parseable"
]
expected_findings = [
  "g3ts-arch/root-manifest-parseable"
]
