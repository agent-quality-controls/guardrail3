id = "R30-root-adoption-pair-complete"
tool = "g3rs"
run_from = "repo"
commands = [
  ["validate", "repo", "--path", ".", "--inventory"],
]
expected_exit = "nonzero"
level = "repo_root_adoption_pair_complete"

valid_state = [
  "repo_root_found",
  "repo_root_directory",
  "repo_root_crawlable",
  "repo_marker_pair_complete",
  "repo_topology_branch_reachable",
]

intentionally_invalid = [
  "repo_topology_nested_workspace",
]
