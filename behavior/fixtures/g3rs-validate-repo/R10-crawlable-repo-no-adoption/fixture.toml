id = "R10-crawlable-repo-no-adoption"
tool = "g3rs"
run_from = "repo"
commands = [
  ["validate", "repo", "--path", ".", "--inventory"],
]
expected_exit = "zero"
level = "repo_root_crawlable_no_adoption"

valid_state = [
  "repo_root_found",
  "repo_root_directory",
  "repo_root_crawlable",
  "repo_markers_absent",
]

intentionally_invalid = []
