id = "R00-invalid-repo-root"
tool = "g3rs"
run_from = "repo"
commands = [
  ["validate", "repo", "--path", "missing", "--inventory"],
  ["validate", "repo", "--path", "README.md", "--inventory"],
]
expected_exit = "nonzero"
level = "repo_root_invalid"

valid_state = []

intentionally_invalid = [
  "repo_root_missing",
  "repo_root_file",
]
