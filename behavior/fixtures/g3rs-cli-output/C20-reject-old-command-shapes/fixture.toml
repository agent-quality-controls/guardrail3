id = "C20-reject-old-command-shapes"
tool = "g3rs"
run_from = "repo"
git_init = true
commands = [
  ["validate-repo"],
  ["validate", "--path", "."],
]
expected_exit = "nonzero"
level = "cli_deleted_command_shapes"

