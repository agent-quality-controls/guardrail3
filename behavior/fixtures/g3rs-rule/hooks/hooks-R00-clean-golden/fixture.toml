id = "hooks-R00-clean-golden"
tool = "g3rs"
run_from = "repo"
commands = [
  ["validate", "repo", "--path", ".", "--inventory"],
]
expected_exit = "zero"
level = "family_rule_clean_golden"

rule_family = "hooks"
target_rules = []
expected_findings = []
