# release_semver_tool_fixture

This fixture isolates the release semver tool availability rule from release metadata rules. The package is otherwise complete enough that the missing `cargo-semver-checks` row is the reason this fixture exists.
