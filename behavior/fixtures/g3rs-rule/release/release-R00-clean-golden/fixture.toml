id = "release-R00-clean-golden"
tool = "g3rs"
run_from = "repo"
commands = [
  ["validate", "workspace", "--path", ".", "--family", "release", "--rules-only", "--inventory"],
]
expected_exit = "zero"
level = "family_rule_clean_golden"

rule_family = "release"
target_rules = []
expected_findings = []
