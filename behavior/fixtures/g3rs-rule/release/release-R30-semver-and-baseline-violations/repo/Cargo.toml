[package]
name = "invalid_semver_fixture"
version = "not-semver"
edition = "2024"
publish = true
description = "Invalid semver fixture"
license = "MIT"
repository = "https://example.com/repo"
keywords = ["fixture"]
categories = ["development-tools"]
include = ["src/**", "Cargo.toml", "README.md", "LICENSE"]

[lib]
path = "src/lib.rs"

[package.metadata.docs.rs]
all-features = true

[workspace]
members = []
resolver = "2"
