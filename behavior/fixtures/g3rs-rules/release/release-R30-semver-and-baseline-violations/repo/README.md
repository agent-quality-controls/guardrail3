# invalid_semver_fixture

This fixture isolates the release valid-semver rule from the larger release metadata fixture. The package is otherwise complete enough that the intended invalid version row is the reason this fixture exists.
