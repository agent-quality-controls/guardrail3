# too_many_keywords

This fixture crate has enough README text and a markdown heading. The crate exists only to expose the release keyword-count branch while keeping the rest of the release metadata valid enough for the fixture layer.
