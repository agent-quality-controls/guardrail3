id = "L50-release-semver-checks-missing"
tool = "g3rs"
run_from = "repo"
commands = [
  ["validate", "workspace", "--path", ".", "--family", "release", "--inventory", "--rules-only"],
]
expected_exit = "zero"
level = "required_inputs_valid_delegated_tools_missing"
runner_mode = "path_without_delegated_tools"

valid_state = [
  "workspace_root_found",
  "guardrail_config_valid",
  "required_inputs_present",
  "required_inputs_valid",
]

intentionally_invalid = [
  "delegated_tools_missing",
]
