id = "L80-project-policy-valid-clean"
tool = "g3rs"
run_from = "repo"
commands = [
  ["validate", "workspace", "--path", ".", "--rules-only", "--inventory"],
]
expected_exit = "zero"
level = "project_policy_valid_clean"

valid_state = [
  "workspace_root_found",
  "guardrail_config_valid",
  "required_inputs_present",
  "required_inputs_valid",
  "delegated_tools_present",
  "delegated_policy_valid",
  "project_policy_valid",
]

intentionally_invalid = []
