id = "L34-deps-library-policy-missing"
tool = "g3rs"
run_from = "repo"
commands = [
  ["validate", "--path", ".", "--family", "deps", "--rules-only", "--inventory"],
]
expected_exit = "zero"
level = "guardrail_config_valid_required_inputs_missing"

valid_state = [
  "workspace_root_found",
  "guardrail_config_valid",
]

intentionally_invalid = [
  "required_inputs_missing",
]
