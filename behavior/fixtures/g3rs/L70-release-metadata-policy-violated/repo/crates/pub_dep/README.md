# pub_dep

This fixture dependency has enough README text to avoid the README quality branch while testing interdependent version consistency. It is deliberately publishable so the release dependency edge checks can prove that local version requirements match the target crate metadata instead of only checking package presence.
