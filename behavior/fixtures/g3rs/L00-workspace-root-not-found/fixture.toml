id = "L00-workspace-root-not-found"
tool = "g3rs"
run_from = "repo"
commands = [
  ["validate", "workspace", "--path", "."],
  ["validate", "workspace", "--path", ".", "--inventory"],
  ["validate", "workspace", "--path", "README.md", "--inventory"],
  ["validate", "workspace", "--path", ".", "--family", "fmt", "--inventory"],
  ["validate", "workspace", "--path", ".", "--family", "apparch", "--inventory"],
  ["validate", "workspace", "--path", ".", "--family", "apparch", "--family", "arch", "--family", "cargo", "--family", "clippy", "--family", "code", "--family", "deps", "--family", "deny", "--family", "fmt", "--family", "garde", "--family", "hooks", "--family", "release", "--family", "test", "--family", "toolchain", "--family", "topology", "--inventory"],
  ["validate", "workspace", "--path", ".", "--staged", "--inventory"],
  ["validate", "workspace", "--path", ".", "--rules-only", "--inventory"],
]
expected_exit = "nonzero"
level = "workspace_root_not_found"

valid_state = []

intentionally_invalid = [
  "workspace_root_not_found",
]
