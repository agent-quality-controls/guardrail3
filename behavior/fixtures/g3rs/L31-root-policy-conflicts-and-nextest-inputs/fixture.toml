id = "L31-root-policy-conflicts-and-nextest-inputs"
tool = "g3rs"
run_from = "repo"
commands = [
  ["validate", "workspace", "--path", ".", "--family", "fmt", "--family", "clippy", "--family", "deny", "--family", "test", "--rules-only", "--inventory"],
]
expected_exit = "nonzero"
level = "guardrail_config_valid_required_inputs_missing"

valid_state = [
  "workspace_root_found",
  "guardrail_config_valid",
]

intentionally_invalid = [
  "required_inputs_missing",
  "required_inputs_invalid",
  "activation_file_conflict",
]
