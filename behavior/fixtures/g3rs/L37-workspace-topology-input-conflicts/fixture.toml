id = "L37-workspace-topology-input-conflicts"
tool = "g3rs"
run_from = "repo"
commands = [
  ["validate", "--path", ".", "--family", "topology", "--family", "cargo", "--rules-only", "--inventory"],
]
expected_exit = "nonzero"
level = "guardrail_config_valid_required_inputs_missing"

valid_state = [
  "workspace_root_found",
  "guardrail_config_valid",
]

intentionally_invalid = [
  "required_inputs_missing",
  "workspace_local_file_misplaced",
  "workspace_topology_invalid",
]
