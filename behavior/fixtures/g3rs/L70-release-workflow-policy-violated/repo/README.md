# release_workflow_fixture

This fixture isolates the release workflow rules from the larger release metadata fixture. The package is otherwise complete enough that the missing workflow rows are the reason this fixture exists.
