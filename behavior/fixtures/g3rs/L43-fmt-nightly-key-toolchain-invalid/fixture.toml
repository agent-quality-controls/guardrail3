id = "L43-fmt-nightly-key-toolchain-invalid"
tool = "g3rs"
run_from = "repo"
commands = [
  ["validate", "workspace", "--path", ".", "--family", "fmt", "--rules-only", "--inventory"],
]
expected_exit = "nonzero"
level = "required_inputs_present_invalid"

valid_state = [
  "workspace_root_found",
  "guardrail_config_valid",
  "required_inputs_present",
]

intentionally_invalid = [
  "required_inputs_invalid",
]
