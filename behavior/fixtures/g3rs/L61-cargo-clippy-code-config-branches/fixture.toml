id = "L61-cargo-clippy-code-config-branches"
tool = "g3rs"
run_from = "repo"
commands = [
  ["validate", "workspace", "--path", ".", "--family", "cargo", "--family", "clippy", "--family", "code", "--rules-only", "--inventory"],
]
expected_exit = "nonzero"
level = "required_inputs_valid_config_policy_invalid"

valid_state = [
  "workspace_root_found",
  "guardrail_config_valid",
  "required_inputs_present",
  "required_inputs_valid",
]

intentionally_invalid = [
  "config_policy_invalid",
]
