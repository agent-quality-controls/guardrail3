id = "L33-release-profile-and-mutants-inputs"
tool = "g3rs"
run_from = "repo"
path_prepend = ["bin"]
commands = [
  ["validate", "workspace", "--path", ".", "--family", "release", "--family", "deps", "--family", "garde", "--family", "toolchain", "--family", "test", "--rules-only", "--inventory"],
]
expected_exit = "nonzero"
level = "guardrail_config_valid_required_inputs_missing"

valid_state = [
  "workspace_root_found",
  "guardrail_config_valid",
]

intentionally_invalid = [
  "activation_file_conflict",
  "required_inputs_missing",
]
