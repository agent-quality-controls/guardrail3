id = "L30-guardrail-config-valid-required-inputs-missing"
tool = "g3rs"
run_from = "repo"
commands = [
  ["validate", "workspace", "--path", ".", "--family", "cargo", "--family", "clippy", "--family", "deny", "--family", "deps", "--family", "fmt", "--family", "toolchain", "--family", "garde", "--rules-only", "--inventory"],
]
expected_exit = "nonzero"
level = "guardrail_config_valid_required_inputs_missing"

valid_state = [
  "workspace_root_found",
  "guardrail_config_valid",
]

intentionally_invalid = [
  "required_inputs_missing",
  "required_inputs_invalid",
]
