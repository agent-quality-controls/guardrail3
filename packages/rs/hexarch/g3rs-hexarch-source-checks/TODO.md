# g3rs-hexarch-source-checks TODO

- extract the next `hexarch` source or config rules after the source slice hardens
- add richer shared assertion helpers if this family grows beyond the current two rules
- keep package tests aligned with old app-family parity as the config slice lands
