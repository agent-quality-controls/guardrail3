# g3rs-hexarch-ingestion TODO

- implement `ingest_for_config_checks(...)` for the remaining non-file-tree `hexarch` rules
- keep member/workspace discovery aligned with old app-family behavior as the config slice lands
- decide whether workspace-member discovery helpers should become shared utility
