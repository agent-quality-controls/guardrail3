# g3rs-hooks-shared-source-checks TODO

- add the next single-file `code` source rules after the first parity slice lands
- add package-level parity smoke tests once `g3rs-hooks-shared-ingestion` exists
- decide whether a separate `g3rs-hooks-shared-types` family crate is needed later
