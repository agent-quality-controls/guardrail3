# g3rs-code-config-checks

Runs the `code` family config checks.

Current rules:

- `g3rs-code/exception-comment-inventory` EXCEPTION comment inventory
- `g3rs-code/unsafe-code-lint` workspace `unsafe_code` lint level
