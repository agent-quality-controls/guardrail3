# g3rs-code-ast-checks TODO

- add the next single-file `code` AST rules after the first parity slice lands
- add package-level parity smoke tests once `g3rs-code-ast-ingestion` exists
- decide whether a separate `g3rs-code-types` family crate is needed later
