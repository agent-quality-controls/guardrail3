# g3rs-test-ingestion TODO

- build `ingest_for_ast_checks` for root-scoped `test` AST bundles
- build `ingest_for_file_tree_checks` for the deferred mixed lane
- add broader package-level parity smoke tests against legacy `test` config behavior
