[package]
name = "g3rs-fmt-config-checks-types"
version = "0.1.0"
edition = "2024"
description = "Public contract types for g3 fmt config checks"
license = "MIT OR Apache-2.0"
rust-version = "1.85"
repository = "https://github.com/ArcticLens/websmasher"
readme = "README.md"
keywords = ["guardrail3", "fmt", "checks", "types"]
categories = ["development-tools"]
include = ["src/**", "Cargo.toml", "README.md", "LICENSE*"]
publish = false

[features]
default = ["api"]
api = []

[lints]
workspace = true

[package.metadata.docs.rs]
all-features = true

[package.metadata.guardrail3]
shared = true

[dependencies]
g3rs-fmt-types = { path = "../../../g3rs-fmt-types" }
