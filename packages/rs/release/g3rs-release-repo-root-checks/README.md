# g3rs-release-repo-root-checks

Live future package for release checks that belong to the monorepo root rather than a Rust workspace root.

Status:
- rule logic and direct tests live here
- not wired into `guardrail3-rs` yet
- waiting for real `ingest_for_repo_root_checks`
