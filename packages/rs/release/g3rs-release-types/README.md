# g3rs-release-types

Shared typed facts for the `release` family.

This package defines the normalized release-family inputs shared by:
- config checks
- filetree checks
- source checks
- ingestion and repo-root checks
