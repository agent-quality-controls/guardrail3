# g3rs-arch-source-checks TODO

- add the next single-file `code` source rules after the first parity slice lands
- add package-level parity smoke tests once `g3rs-arch-ingestion` exists
- decide whether a separate `g3rs-arch-types` family crate is needed later
