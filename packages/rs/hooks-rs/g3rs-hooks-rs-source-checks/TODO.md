# g3rs-hooks-rs-source-checks TODO

- add the next single-file `code` source rules after the first parity slice lands
- add package-level parity smoke tests once `g3rs-hooks-rs-ingestion` exists
- decide whether a separate `g3rs-hooks-rs-types` family crate is needed later
