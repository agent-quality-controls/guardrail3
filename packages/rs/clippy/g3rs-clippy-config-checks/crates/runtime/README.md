# g3rs-clippy-config-checks-runtime

Runtime implementation for `g3rs-clippy-config-checks`.

This crate contains the rule execution logic for the clippy config-check family.
It depends on the family-owned public input types and emits guardrail check
results.
