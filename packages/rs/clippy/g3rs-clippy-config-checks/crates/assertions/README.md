# g3rs-clippy-config-checks-assertions

Assertion helpers for `g3rs-clippy-config-checks` tests.

This crate exposes reusable result-shape assertions for clippy config-check
rules without depending on runtime internals.
