# TODO

- add real file-tree ingestion for local path target validation
- add richer normalization around external path dependency identity when file-tree facts exist
- add broader real-workspace ingestion tests
