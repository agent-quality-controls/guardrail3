# g3rs-hooks-source-checks TODO

- keep parity with the old hook ledgers while the merged source lane stays normalized as `RS-HOOKS-SOURCE-*`
- tighten end-to-end coverage as new hook file-tree work lands in the merged `hooks` family
