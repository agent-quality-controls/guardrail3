[package]
name = "g3rs-hooks-config-checks-types"
version = "0.1.0"
edition = "2024"
description = "Public types for g3rs hooks config checks"
license = "MIT OR Apache-2.0"
rust-version = "1.85"
repository = "https://github.com/ArcticLens/websmasher"
readme = "README.md"
keywords = ["guardrail3", "hooks", "config", "checks", "rust"]
categories = ["development-tools"]

[features]
default = ["all"]
all = ["api"]
api = []

[lints]
workspace = true

[dependencies]
g3rs-hooks-types = { path = "../../../g3rs-hooks-types" }
