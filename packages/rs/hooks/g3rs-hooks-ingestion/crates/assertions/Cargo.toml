[package]
name = "g3rs-hooks-ingestion-assertions"
version = "0.1.0"
edition = "2024"
description = "Assertions for g3rs hooks ingestion tests"
license = "MIT OR Apache-2.0"
rust-version = "1.85"
repository = "https://github.com/ArcticLens/websmasher"
readme = "README.md"
keywords = ["guardrail3", "hooks", "ingestion", "rust"]
categories = ["development-tools"]

[features]
default = ["all"]
all = ["checks"]
checks = []

[lints]
workspace = true

[dependencies]
g3rs-hooks-source-checks-types = { path = "../../../g3rs-hooks-source-checks/crates/types", version = "0.1.0" }
