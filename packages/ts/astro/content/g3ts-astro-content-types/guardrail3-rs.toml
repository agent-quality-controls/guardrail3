profile = "library"

[checks]
garde = false

[[waivers]]
rule = "g3rs-code/public-struct-named-fields"
file = "src/types.rs"
selector = "struct:G3TsAstroContentPolicySnapshot"
reason = "Type package exposes typed DTOs consumed by separate ingestion and check crates; fields are the public package contract."

[[waivers]]
rule = "g3rs-code/too-many-use-imports"
file = "src/lib.rs"
selector = "top-level-use-imports"
reason = "Type package facade explicitly re-exports the public DTO contract; broad re-exports are forbidden and splitting the contract would hide package ownership."

[[waivers]]
rule = "g3rs-code/public-struct-named-fields"
file = "src/types.rs"
selector = "struct:G3TsAstroPackageSurfaceSnapshot"
reason = "Type package exposes typed DTOs consumed by separate ingestion and check crates; fields are the public package contract."

[[waivers]]
rule = "g3rs-code/public-struct-named-fields"
file = "src/types.rs"
selector = "struct:G3TsAstroPackageScriptCommand"
reason = "Type package exposes typed DTOs consumed by separate ingestion and check crates; fields are the public package contract."

[[waivers]]
rule = "g3rs-code/public-struct-named-fields"
file = "src/types.rs"
selector = "struct:G3TsAstroPackageScriptToolInvocation"
reason = "Type package exposes typed DTOs consumed by separate ingestion and check crates; fields are the public package contract."

[[waivers]]
rule = "g3rs-code/public-struct-named-fields"
file = "src/types.rs"
selector = "struct:G3TsAstroContentEslintSurfaceSnapshot"
reason = "Type package exposes typed DTOs consumed by separate ingestion and check crates; fields are the public package contract."

[[waivers]]
rule = "g3rs-code/public-struct-named-fields"
file = "src/types.rs"
selector = "struct:G3TsAstroContentPolicyEslintContractInput"
reason = "Type package exposes typed DTOs consumed by separate ingestion and check crates; fields are the public package contract."

[[waivers]]
rule = "g3rs-code/public-struct-named-fields"
file = "src/types.rs"
selector = "struct:G3TsAstroContentIntegrationContractInput"
reason = "Type package exposes typed DTOs consumed by separate ingestion and check crates; fields are the public package contract."

[[waivers]]
rule = "g3rs-code/public-struct-named-fields"
file = "src/types.rs"
selector = "struct:G3TsAstroContentConfigChecksInput"
reason = "Type package exposes typed DTOs consumed by separate ingestion and check crates; fields are the public package contract."

[[waivers]]
rule = "g3rs-code/public-struct-named-fields"
file = "src/types.rs"
selector = "struct:G3TsAstroContentFileTreeChecksInput"
reason = "Type package exposes typed DTOs consumed by separate ingestion and check crates; fields are the public package contract."
