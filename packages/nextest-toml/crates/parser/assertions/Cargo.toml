[package]
name = "nextest-toml-parser-assertions"
version = "0.0.0"
edition = "2024"
description = "Assertion helpers for nextest-toml parser tests"
license.workspace = true
repository.workspace = true
publish = true
readme = "README.md"
keywords = ["nextest", "toml", "assertions", "parser", "rust"]
categories = ["config", "development-tools", "parser-implementations"]

[lints]
workspace = true

[features]
default = ["all"]
all = ["checks"]
checks = []

[dependencies]
nextest-toml-parser = { path = "../runtime" }
