[package]
name = "g3rs-deny-config-ingestion-runtime"
version = "0.1.0"
edition = "2024"
description = "Runtime implementation for g3rs deny config ingestion"
license = "MIT OR Apache-2.0"
rust-version = "1.85"
repository = "https://github.com/ArcticLens/websmasher"
readme = "README.md"
keywords = ["guardrail3", "deny", "ingestion", "rust"]
categories = ["development-tools"]

[features]
default = ["all"]
all = ["ingest"]
ingest = []

[lints]
workspace = true

[package.metadata.guardrail3]
shared = true

[dependencies]
deny-toml-parser = { path = "../../../deny-toml-parser", version = "0.1.0" }
g3rs-deny-config-checks = { path = "../../../g3rs-deny-config-checks", version = "0.1.0" }
g3rs-deny-config-ingestion-types = { path = "../types", version = "0.1.0" }
g3rs-workspace-crawl = { path = "../../../g3rs-workspace-crawl", version = "0.1.0" }

[dev-dependencies]
g3rs-deny-config-ingestion-assertions = { path = "../assertions" }
tempfile = "3"
