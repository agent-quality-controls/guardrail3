[package]
name = "nextest-toml-parser-runtime-assertions"
version.workspace = true
edition = "2024"
description = "Assertion helpers for nextest-toml parser tests"
license.workspace = true
repository.workspace = true
publish = false
readme = "README.md"
keywords = ["nextest", "toml", "assertions", "parser", "rust"]
categories = ["config", "development-tools", "parser-implementations"]

[lints]
workspace = true

[features]
default = ["all"]
all = ["checks"]
checks = []

[dependencies]
nextest-toml-parser-runtime = { path = "../runtime" }

[package.metadata.docs.rs]
all-features = true
