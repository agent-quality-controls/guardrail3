[package]
name = "mutants-toml-parser-runtime-assertions"
version.workspace = true
edition = "2024"
description = "Assertion helpers for mutants-toml parser tests"
license.workspace = true
repository.workspace = true
publish = false
readme = "README.md"
keywords = ["mutants", "toml", "assertions", "parser", "rust"]
categories = ["config", "development-tools", "parser-implementations"]

[lints]
workspace = true

[features]
default = ["all"]
all = ["checks"]
checks = []

[dependencies]
mutants-toml-parser-runtime = { path = "../runtime" }

[package.metadata.docs.rs]
all-features = true
