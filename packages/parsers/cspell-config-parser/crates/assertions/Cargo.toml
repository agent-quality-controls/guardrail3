[package]
name = "cspell-config-parser-runtime-assertions"
version = "0.0.0"
edition.workspace = true
description = "Assertions for cspell config parser runtime"
license.workspace = true
repository.workspace = true
publish = false
readme = "README.md"

[dependencies]
cspell-config-parser-runtime = { path = "../runtime", version = "0.1.0" }

[features]
default = ["all"]
all = ["checks"]
checks = []

[lints]
workspace = true
