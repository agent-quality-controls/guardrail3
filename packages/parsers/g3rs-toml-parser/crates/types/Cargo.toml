[package]
name = "g3rs-toml-parser-types"
version.workspace = true
edition = "2024"
description = "Typed model for guardrail3-rs.toml files"
license.workspace = true
rust-version.workspace = true
repository.workspace = true
keywords = ["guardrail3", "rust", "toml", "types", "parser"]
categories = ["config", "development-tools", "parser-implementations"]
publish = false
readme = "README.md"
include = ["src/**", "Cargo.toml", "README.md", "LICENSE*"]

[package.metadata.guardrail3]
shared = true

[lints]
workspace = true

[features]
default = ["all"]
all = ["api"]
api = []

[dependencies]
guardrail3-waivers = { path = "../../../../shared/guardrail3-waivers", version = "0.1.0" }
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[package.metadata.docs.rs]
all-features = true
