[package]
name = "rustfmt-toml-parser-runtime"
version.workspace = true
edition = "2024"
description = "Parser implementation for rustfmt.toml facade packages"
license.workspace = true
rust-version.workspace = true
repository.workspace = true
keywords = ["rustfmt", "toml", "parser", "configuration", "rust"]
categories = ["config", "development-tools", "parser-implementations"]
publish = false
readme = "README.md"
include = ["src/**", "Cargo.toml", "README.md", "LICENSE*"]

[lints]
workspace = true

[features]
default = ["all"]
all = ["api"]
api = []

[dependencies]
rustfmt-toml-parser-types = { path = "../types", version = "0.1.0" }
toml = "0.8"

[package.metadata.docs.rs]
all-features = true
