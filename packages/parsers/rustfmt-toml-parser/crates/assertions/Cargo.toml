[package]
name = "rustfmt-toml-parser-runtime-assertions"
version.workspace = true
edition = "2024"
description = "Assertion helpers for rustfmt-toml parser tests"
license.workspace = true
repository.workspace = true
publish = false
readme = "README.md"
keywords = ["rustfmt", "toml", "assertions", "parser", "rust"]
categories = ["config", "development-tools", "parser-implementations"]

[lints]
workspace = true

[features]
default = ["all"]
all = ["checks"]
checks = []

[dependencies]
rustfmt-toml-parser-runtime = { path = "../runtime" }
tempfile = "3"

[package.metadata.docs.rs]
all-features = true
