[package]
name = "jscpd-json-parser-runtime"
version.workspace = true
edition = "2024"
description = "Parser implementation for .jscpd facade packages"
license.workspace = true
rust-version.workspace = true
repository.workspace = true
keywords = ["typescript", "jscpd", "parser", "config", "guardrail3"]
categories = ["development-tools", "parser-implementations"]
publish = false
readme = "README.md"
include = ["src/**", "Cargo.toml", "README.md", "LICENSE*"]

[lints]
workspace = true

[features]
default = ["all"]
all = ["api"]
api = []

[dependencies]
jscpd-json-parser-types = { path = "../types", version = "0.1.0" }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
jscpd-json-parser-runtime-assertions = { path = "../assertions" }

[package.metadata.docs.rs]
all-features = true
