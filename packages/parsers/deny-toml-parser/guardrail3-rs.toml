profile = "library"

allowed_deps = [
  "deny-toml-parser-runtime",
  "deny-toml-parser-types",
  "serde",
  "toml",
]

[checks]
garde = false

[[waivers]]
rule = "RS-CODE-SOURCE-19"
file = "crates/types/src/bans.rs"
selector = "struct:BansConfig"
reason = "Schema mirror intentionally matches the deny.toml bans section surface."
