# deny-toml-parser-runtime-assertions

Shared proof helpers for `deny-toml-parser-runtime` sidecar tests.

The runtime sidecars should finish through this crate so internal and external
tests use the same proof surface.
