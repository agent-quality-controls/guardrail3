[package]
name = "g3ts-toml-parser-runtime-assertions"
version.workspace = true
edition = "2024"
description = "Assertion helpers for guardrail3-ts.toml parser tests"
license.workspace = true
repository.workspace = true
publish = false
readme = "README.md"
keywords = ["guardrail3", "typescript", "toml", "assertions", "parser"]
categories = ["config", "development-tools", "parser-implementations"]
include = ["src/**", "Cargo.toml", "README.md", "LICENSE*"]

[lints]
workspace = true

[features]
default = ["all"]
all = ["checks"]
checks = []

[dependencies]
g3ts-toml-parser-runtime = { path = "../runtime" }

[package.metadata.docs.rs]
all-features = true
