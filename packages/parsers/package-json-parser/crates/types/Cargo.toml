[package]
name = "package-json-parser-types"
version.workspace = true
edition = "2024"
description = "Typed model for parsed package.json documents"
license.workspace = true
rust-version.workspace = true
repository.workspace = true
keywords = ["typescript", "package-json", "types", "json", "guardrail3"]
categories = ["development-tools", "parser-implementations"]
publish = false
readme = "README.md"
include = ["src/**", "Cargo.toml", "README.md", "LICENSE*"]

[package.metadata.guardrail3]
shared = true

[lints]
workspace = true

[features]
default = ["all"]
all = ["api"]
api = []

[dependencies]
serde_json = "1"

[package.metadata.docs.rs]
all-features = true
